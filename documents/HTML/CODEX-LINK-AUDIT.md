# تدقيق الرابط: الاسم والوجهة — ١ سبتمبر ٢٠٢٦

فُحصت روابط وعناصر الفتح في `admin` و`console` و`modonty` (1176 سطراً مرشحاً لـ`a`/`Link`/`href`). لم أجد حالة باقية يكون فيها اسم الرابط يعد بكونسول أو وجهة أخرى ثم يرسله إلى موقع مختلف، ولم أجد تكرار وجهة بلا سبب في الشاشة نفسها. الروابط السليمة لا تُسرد؛ العدد أعلاه يشملها.

## الاسم يكذب

لا شيء.

## أمني

| التطبيق | الملف:سطر | النصّ | الوجهة | الحكم |
|---|---|---|---|---|
| admin | `components/shared/media-picker-dialog.tsx:217-222` | `Upload` | `/media/upload?clientId=${clientId}` في تبويب جديد | `target="_blank"` بلا `rel="noopener noreferrer"`. |

## نطاق ثابت

| التطبيق | الملف:سطر | النصّ | الوجهة | الحكم |
|---|---|---|---|---|
| console | `app/help/HelpClient.tsx:37-43` | `الصفحة العامة` | `https://www.modonty.com/story` | رابط داخلي إلى منتج Modonty مثبت على نطاق الإنتاج، لا يأخذ متغير البيئة؛ ينكسر في المحلي/بيئة معاينة. |

## التكرار وإتاحة الوصول

| التطبيق | الملف:سطر | النصّ | الوجهة | الحكم |
|---|---|---|---|---|
| — | — | — | — | لا رابط مكرر غير مبرر، ولا زر فتح مرشح بلا نص ولا `aria-label` في العناصر التي تفتح رابطاً. |

## ملاحظات فحص

- الروابط الرسمية إلى جهات خارج المنتج (مثل Google، Bing، YouTube، واتساب، وJBR SEO) ليست «نطاقاً ثابتاً يكسر المحلي»، لذلك لم تُسجّل كمخالفة بيئية.
- مسارات `route.ts`، رسائل البريد، والـAPI ليست عناصر رابط معروضة في واجهة؛ لم تُعامل كـ`<a>`/`<Link>` أو زر فتح.
