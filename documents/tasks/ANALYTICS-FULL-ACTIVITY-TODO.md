# Analytics Full-Activity — تتبع المهام

## 🚀 خطة الدفع المعتمدة: الأدمن فقط أولاً (2026-07-08 — سؤال خالد «بنقدر نرفع بس الـ admin؟» → مؤكد بالدليل)
- **التأكيد:** كود الأدمن صفر اعتماد على السكيما الجديدة (ريلز/geo) أو Bunny — grep شامل بلا نتائج. السكيما تظل غير مدفوعة.
- **الآلية:** commit انتقائي لـ `admin/**` + `documents/**` فقط؛ الـ 11 ملف المعدل خارج الأدمن (كونسول 3 + مودونتي 7 + schema.prisma) تظل محلية. ignoreCommand يخلي كونسول/مودونتي CANCELED على Vercel.
- [x] **الكاسر الأخير انتفى — مفاتيح GA4 موجودة على Vercel ومربوطة بالأدمن (تحقق 2026-07-08، ترقيم كامل 66 متغير/3 صفحات):** GA4_PROPERTY_ID (القيمة 538167732 مطابقة للمحلي) + GA4_CLIENT_EMAIL + GA4_PRIVATE_KEY_BASE64، كلها production. **دفع الأدمن سالك بلا شروط.**
- خطوات قبل الدفع: [ ] حذف الشواهد (الأمر أدناه، مع استثناء modonty/components/tracked-cta-link.tsx من الـ add) · [ ] tsc + build أدمن · [ ] تست حي · [ ] backup · [ ] version bump + changelog · [ ] عدم `git add .` أبداً (مجلد modonty/.next-stale-* غير متجاهل).
- **يُدفع لاحقاً (دفعة ٢):** كونسول gallery→Bunny (يتطلب rotation مفتاح Bunny الرئيسي + مفاتيح BUNNY_* على Vercel) + مودونتي (geo tracking + booking_submit + صفحة reels — حالياً غير مربوطة بأي قائمة) + schema.prisma.

## ✅ تدقيق صحة البيانات 100% (2026-07-08 — طلب خالد «review + تأكيد 100%»)
- [x] **مراجعة كود كاملة** لخط البيانات: get-ga4-activity (13 استعلام) · get-leads-detail (GA4 tiles + 5 جداول DB) · visitor-actions-breakdown · full-activity-client (رياضيات العرض) · ga4-data-api (JWT/property 538167732).
- [x] **تحقق مستقل GA4**: سكربت منفصل (scratchpad/verify-ga4.mjs) أعاد اشتقاق كل رقم من الـ Data API مباشرة — **كل الأرقام الـ 11 المعروضة طابقت رقماً برقم** (1,715 / 540 / 644 / 489 / 36 / 6 + tiles 0/4/2/1).
- [x] **تحقق مستقل DB** (verify-db-counts.mjs على modonty_dev بعد طباعة الـ URL): bookings 9 · messages 2 · questions 0 · comments 7 · newStatus 15 (7+2+0+4+2) — مطابق للمعروض. كل الجداول ≤300 (سقف take آمن حالياً).
- [x] **حالات الـ status متحققة من السكيما**: booking/contact = "new" · FAQ/comments = "PENDING" — فلتر newStatus صحيح.
- [x] 🐛 **باق حقيقي اكتُشف وأُصلح: تضخيم Geography** — جمع totalUsers عبر صفوف المدن يضخّم الدولة (الزائر يظهر بأكثر من مدينة): مصر 2133→الحقيقي 2035، السعودية 147→132. الإصلاح: استعلام دولة-فقط للعدّ + استعلام المدن للتفصيل فقط (get-ga4-activity). تحقق حي بعد الإصلاح: 132/2035 ✅.
- [x] تصحيحات تسمية: KPI hint «everything, all types»→«tracked business events» (الرقم يستثني web_vitals/page_view الخام) · «views»→«users» في Geography · إضافة «last 90 days» لعنوان صف أفعال الزوار (نافذته 90 يوم بينما KPIs = 30 يوم).
- [x] إصلاح ثانوي: فلتر العميل keepClient كان startsWith (slug «foo» يلتقط «foobar») → مطابقة جذر دقيقة.
- [x] Geography: السعودية ومصر مثبّتتان دائماً، الباقي + Unknown في سطر مطوي (طلب خالد).
- **⚠️ محاذير موثقة (مش أخطاء):** أرقام GA4 تتأخر 24-48 ساعة عن اللحظي · Bookings من GA4 = 0 حتى أول deploy لـ booking_submit (الـ 37 conversion_complete القديمة لا تُحتسب عمداً) · فشل GA4 في tiles يعرض أصفار بدل تعطيل الصفحة (fallback مقصود) · KPI «Visitor Actions» (30 يوم، 6 أحداث) ≠ صف الأفعال (90 يوم، 4 أنواع) — تعريفان مختلفان بعناوين مختلفة.

## ✅ الدمج النهائي: الرئيسية = الترمومتر (2026-07-08 — موكب dashboard-thermometer-merge-v1 معتمد «do»)
- [x] الرئيسية `/` صارت الصفحة الموحدة: تنبيهات ← **صف أفعال الزوار المفصل** (يحتاج إجراء/حجوزات/رسائل/أسئلة/تعليقات — مكوّن جديد يعيد استخدام getLeadsDetail) ← نشاط GA4 كامل (FullActivityClient كما هو — صفر تكرار) ← سير عمل المقالات ← Search Console.
- [x] **الحشو المحذوف:** قسم GA4 «تحت الإنشاء» + فلتر التاريخ المكرر + قسم DB/Ops من الرئيسية + بند Analytics من الشريط + `/analytics` تحوّل للرئيسية (drill-downs باقية).
- [x] **الكود الميت:** 12 ملفاً حُوّلت شواهد سطرية (`export {}`) + تقليم barrel — **أوامر الحذف محظورة بالجلسة؛ أمر الحذف اليدوي جاهز أدناه**. النوع `FullActivity` انتقل لـ`get-ga4-activity.ts`. (أبقيت `get-analytics-data.ts` — تستخدمه صفحة العملاء.)
- [x] TSC صفر ×2 + تست حي (الرئيسية + التحويلة + التفصيل).
- ⚠️ **انحرافان صغيران عن الموكب (يحتاجان مباركة خالد أو إعادة هيكلة):** ١) صف أفعال الزوار قبل بطاقات GA4 (الموكب: بعدها) ٢) فلتر التاريخ داخل بلوك النشاط مش برأس الصفحة — سببهما إعادة استخدام FullActivityClient كوحدة واحدة.

**أمر حذف الشواهد (يلصقه خالد):**
```
cd c:/Users/w2nad/Desktop/dreamToApp/MODONTY
rm "admin/app/(dashboard)/analytics/components/analytics-page-client.tsx" "admin/app/(dashboard)/analytics/components/analytics-charts.tsx" "admin/app/(dashboard)/analytics/components/views-chart.tsx" "admin/app/(dashboard)/analytics/components/traffic-sources-chart.tsx" "admin/app/(dashboard)/analytics/components/top-articles-chart.tsx" "admin/app/(dashboard)/analytics/components/export-button.tsx" "admin/app/(dashboard)/analytics/actions/get-views-trend-data.ts" "admin/app/(dashboard)/analytics/actions/get-full-activity.ts" "admin/app/(dashboard)/components/date-range-selector.tsx" "admin/app/(dashboard)/components/sections/db-section.tsx" "modonty/components/tracked-cta-link.tsx"
```

> انطلق من جلسة نقاش 2026-07-07 (تيليقرام ↔ التحليلات). الموكب المعتمد: `documents/mockups/analytics-redesign-v1.html` («confirm» 2026-07-07).

**Last Updated: 2026-07-08**

## ✅ Done

- [x] **تشخيص الفجوة بالدليل من الإنتاج:** الصفحة القديمة تعرض 517 من 1,547 حدثاً شهرياً (زيارات صفحات الشركات 518 + CTA 480 غير معروضة إطلاقاً).
- [x] **إصلاح جذر تصنيف المصادر (كان كل شيء «عضوي» زوراً):** المتتبعان يرسلان `document.referrer` + رابط الصفحة (كان السيرفر يقرأ ريفرر الطلب الداخلي = صفحة المقال نفسها). مصنّف موحد جديد `modonty/lib/analytics/classify-source.ts`: UTM أولاً ثم محركات بحث/سوشال/إيميل/إحالة + قيمة `INTERNAL` الجديدة. **البيانات التاريخية للمصادر غير قابلة للاسترجاع** — النظيف من 2026-07-07 (ثابت `CLEAN_SINCE`).
- [x] **الجغرافيا (بلد + منطقة + مدينة):** حقول جديدة على `Analytics` + `ClientView` + التقاط من رؤوس فيرسل الرسمية (`x-vercel-ip-country/-country-region/-city`، المدينة تُفك ترميزها) — مجاني بلا خدمة خارجية. تتراكم من أول نشرة إنتاج (فارغة محلياً).
- [x] **`ClientView` صار مصنّفاً:** source/referrerDomain/searchEngine أضيفت له (كان خام فقط).
- [x] **إعادة بناء صفحة `/analytics` بالأدمن** حسب الموكب المعتمد: 6 بطاقات نشاط كامل + خط زمني ثلاثي + مصادر (مع وسم النظافة) + 🌍 جغرافيا + جدول المقالات×المصادر (مع mix bar وأهم بلد وCTA) + جدول صفحات الشركات + فلتر عميل + تصدير CSV. (`actions/get-full-activity.ts` + `components/full-activity-client.tsx` + `page.tsx`.)
- [x] TSC صفر (admin + modonty) + تست حي على dev (221 حدثاً معروضة مقابل 44 سابقاً).
- [x] **فلتر المقالات** (ملاحظة خالد على مطابقة الموكب): قائمة «All articles» جنب «All clients»، تتقيد بالعميل المحدد، واختيار مقال يعمّق البطاقات له (زيارات صفحات الشركات تصفّر — مش أحداث مقال). متحقق حياً: 35 مقالاً، الفلتر يعيد البطاقات لنطاق المقال الواحد.

- [x] **التحول لـGA4 كمصدر الحقيقة** («Google always the SOT» — خالد 2026-07-07): عميل GA4 Data API بالأدمن (منقول من الكونسول المجرّب — `admin/lib/analytics/ga4-data-api.ts`) + أكشن `get-ga4-activity.ts` بنفس شكل البيانات فالواجهة ما تغيرت. 12 استعلاماً على دفعتين (حصة GA4). أسماء الأحداث موثقة من `events-registry.ts` (CTA = `outbound_click`).
- [x] **تنظيف ضجيج القياس السيرفري:** أحداث Measurement Protocol كانت تغرق المصادر بـ«Unassigned 79%» والجغرافيا بـ(not set) — قُصرت استعلامات المصادر/الجغرافيا على `page_view` الحقيقي → Unassigned هبط من 12,812 لـ10.
- [x] أسماء الدول بدل الأعلام فقط (ويندوز يرسم أعلام الإيموجي حروفاً) — مفتاح `CC|Name`.
- [x] تحقق حي بأرقام الإنتاج عبر GA4: 1,803 حدث/30يوم · مصر 88% (القاهرة 354) · السعودية 6% (جدة 72) · سوشال 58% (فيسبوك 1,785).

## 💡 إنسايت تجاري من أول تشغيل (2026-07-07)
**88% من الزوار مصر (سوشال/فيسبوك بالغالب) و6% فقط السعودية** — بينما السوق المستهدف الأول سعودي. أول سؤال استراتيجي للتحليلات الجديدة جاهز.

- [x] **صفحات التفصيل الثلاث (2026-07-07 — «البطاقة باب»):** `/analytics/leads` (حجوزات + رسائل + أسئلة القراء، 90 يوماً، أسماء/هواتف/حالات/مقال المصدر + عدّاد «يحتاج إجراء» + روابط لصفحات الرسائل) · `/analytics/cta` (يومي + أعلى الصفحات + المصادر من GA4 + آخر 50 نقرة بأنواعها من قاعدتنا) · `/analytics/engagement` (بالنوع/اليوم/الصفحة من GA4 + آخر التعليقات واللايكات) — والبطاقات الثلاث صارت روابط. TSC صفر + الثلاث صفحات 200 + تست حي لصفحة الليدز (9 حجوزات كاملة التفاصيل).
- [x] قاعدتا «GA4 SOT» و«الترمومتر» مسجلتان بالذاكرة.
- [x] **تصحيح مفهوم CTA (ملاحظة خالد 2026-07-07 — «CTA اللي أقصده: حجز، رسالة للدكتور، تعليق»):** صفحة الليدز توسعت وصارت **«Visitor Actions — أفعال الزوار»** بإضافة التعليقات (مقالات + صفحات شركات) = نوع رابع بجدول موحد + بطاقة خامسة. بطاقة النظرة العامة «CTA Clicks» أعيدت تسميتها «Button Clicks» و«Leads» صارت «Visitor Actions». متحقق حياً: يحتاج إجراء 15 · حجوزات 9 · تعليقات 7.

## ✅ مراجعة دائرة الحجز بنوعيها (2026-07-07 — طلب خالد «حل كامل»)
- [x] **حدث `booking_submit` أضيف:** كل حجز نموذج يرسل لـGA4 حدثاً باسمه (client_id/slug/name + booking_source + article_id) → عدّ Google = عدّ قاعدتنا. وسلة Leads في `get-ga4-activity` تحسبه وتستبعد `conversion_complete` (الحجز يطلق الاثنين — منع عد مزدوج).
- [x] **تدقيق تتبع «تسوّق الآن» (نوع الرابط) — تصحيح إنذار خاطئ:** الفحص الأول (بحث عن الدالة الخام) أوحى بثماني واجهات عمياء؛ الفحص الصحيح أثبت أن **كل الواجهات متتبعة أصلاً** عبر `CtaTrackedLink` / `ClientCardCta` / `ClientContactSheet` (الشريط السفلي يفتح الشيت المتتبع) → DB + GA4 outbound_click + تيليقرام. درس مسجل: التتبع قد يكون داخل مكوّن مغلف — افحص المغلفات قبل الحكم.
- [x] قرار مقفل: نقرة رابط المتجر **ليست ليداً** (بلا هوية) — مكانها Button Clicks وجدول الشركات، مش صفحة أفعال الزوار.
- [x] **معمار خالد لصفحة أفعال الزوار (2026-07-07):** كروت الأعداد من **GA4** (حجز/رسائل/أسئلة/تعليقات — مع رقم قاعدتنا صغيراً تحتها للشفافية) والضغط يفلتر **جدول قاعدة البيانات** بالتفاصيل. «يحتاج إجراء» يظل من قاعدتنا (Google بلا حالات). GA4 fallback آمن (فشله ما يفرغ الصفحة). متحقق حياً.
- [x] دائرة حجز النموذج (YMYL) مفحوصة كاملة وسليمة: تحقق + إقرار سيرفري إلزامي + مضاد سبام + DB + إشعار + إيميل للطبيب + تيليقرام + GA4.
- [ ] ملف مخلف للحذف اليدوي: `modonty/components/tracked-cta-link.tsx` (مكرر أُنشئ ثم اكتُشف الأصلي — أوامر الحذف محظورة بالجلسة).
- [ ] تعليق سكيما مضلل على `BookingRequest` («+ warm-lead LINK clicks» — غير منفذ وقُرر عدم تنفيذه) — يُصحح مع أول لمسة سكيما قادمة (تجنباً لرقصة بريزما لتعليق).

## 🔍 مراجعة GA4 (2026-07-07) — الناقص المقترح إضافته
- [ ] **أحداث الريلز — صفر تتبع GA4!** الفيد الجديد ما يرسل شيئاً: reel_view · reel_like · reel_favorite · reel_share (+ الوصول لنهاية الفيد). أهم إضافة قادمة.
- [ ] **تسجيل الأبعاد المخصصة بخاصية GA4** (من الداشبورد — خالد): `client_id` مسجل ويعمل؛ التحقق من `article_id` و`category`/`author` — يفتح CTA لكل عميل بجدول الشركات (البند المعلق).
- [ ] **Enhanced Measurement** (داشبورد GA4 — خالد يتحقق من تفعيله): scroll 90% · site search (`/search?q=`) · outbound.
- [ ] **user_id للمسجلين** — الكود يدعمه (options.userId) — تأكيد وصوله فعلياً.

## ⏳ Pending
- [ ] `get-full-activity.ts` (نسخة قاعدة بياناتنا) صارت غير مستخدمة بعد التحول لـGA4 — إبقاء كمرجع/fallback ولا حذف؟ (قرار خالد)
- [ ] عمولة CTA لكل عميل من GA4 تحتاج البعد المخصص `customEvent:client_id` بالاستعلام (مرحلة ثانية) — حالياً 0 بجدول الشركات.
- [ ] «1 Issue» بأوفرلاي التطوير = ضجيج كوكي الجلسة القديم (JWTSessionError) — مش من الصفحة.

- [ ] **إحياء الحدثين الميتين بتيليقرام:** `leadHigh` + `campaignInterest` — معرفان بالكتالوج بلا أي مطلق (طلب خالد الصريح).
- [ ] **البوت الإداري يستقبل كل الأحداث:** المفتاح موجود (`Settings.telegramAdminMirrorAll`) — خالد يفعّله من الأدمن أو نغيّر الافتراضي (قرار بسيط معلق).
- [ ] **سجل الأحداث الموحد** (طلب خالد الأصلي): جدول واحد يلتقط كل حدث عبر قمع `notifyTelegram` — للتحليلات المستقبلية العميقة. (الصفحة الحالية تتجمع من الجداول المتخصصة وتكفي المرحلة.)
- [ ] المكونات القديمة المستبدلة في `analytics/components` (views-chart, traffic-sources-chart, top-articles-chart, analytics-charts, analytics-page-client) + `get-analytics-data.ts` — **كود ميت ينتظر الحذف** (أوامر الحذف مرفوضة بالجلسة — يحذفها خالد أو جلسة قادمة بإذن).
- [ ] عمود «Bookings / Top source» بجدول الشركات — تحسين تباعد بسيط.
- [ ] تست حي بعد النشر للإنتاج: التحقق من امتلاء الجغرافيا والمصادر النظيفة خلال 48 ساعة.
