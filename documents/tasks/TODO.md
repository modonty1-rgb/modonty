# ✅ TODO — ملف المهام الوحيد (مرقّم — أعطني الرقم فقط)

> **القاعدة (خالد 2026-07-08):** هذا هو ملف الـ TODO الوحيد. أي مهمة/«reminder»/بند مريم يدخل هنا. ممنوع ملف TODO جديد.
> **ما ليس TODO يبقى بمكانه:** `documents/reels/` · `documents/content-team/` · المراجع · `documents/tasks/CLAUDE.md` · SESSION-LOG.
> **الأرقام مرجعية ثابتة** بيني وبين خالد — البند المنجز يُحذف والباقي يبقى برقمه (بلا إعادة ترقيم).

**Last Updated: 2026-08-02 (سيو صفحات مودونتي مقفول ١٠٠٪ — مولّد واحد مدقّق للسبع صفحات + تحقّق حيّ على DOM · الكود الميت أُزيل بالكامل (١٦ ملف سلسلة article-seo-config + seo-overview-actions + savePageSeo) + ٦ أعمدة سكيما يتيمة · بندان جديدان 71/72 · بند 70 السابق — سقف المقالات رُفع لـ`null` + باق `canonical` الخطير أُصلح بعشر حالات مدروسة + سجل تدقيق + الإصلاحات القياسية صارت تلقائية بسطر واحد وزر تقرير · `tsc` الأدمن صفر أخطاء · سابقاً فجراً — regenerate الشامل على التست: ٤ مراحل مقفولة بفحص Google (تصنيفات·وسوم·صناعات·عملاء = صفر ملاحظات) + زرّ Cascade صار checkbox+Cancel + بندان جديدان 68/69 · سابقاً 2026-08-01 ليلاً — T6 اكتمل: push version-2 + تست preview نظيف + Rich Results ✓ · بند 68 أُنجز فوراً بأمر خالد: المولّدان أصلحا (MedicalClinic + telephone + priceRange افتراضي "$" + image fallback) + المُراجع اليمّي صار Person بدل Physician/Attorney (`build-ymyl-jsonld.ts` — Physician نوع LocalBusiness فكان Google يطالب شخصاً بحقول محل) + regen على التست + إعادة فحص Rich Results = **صفر ملاحظات نهائياً على المقال كله** · بند 66 اللايف · بند 67 إعادة السرعة 08-05)** · **📄 المرجع الحيّ الوحيد للمسار: `documents/tasks/BUNNY-GOLIVE-FLOW-v1.html`** — المتبقّي المفتوح فقط (T6 اكتمل 2026-08-01): **`T7`** ترحيل صور الإنتاج ← **`T8`** السيو (محروس: لا توليد على الإنتاج قبل تأكيد الصور) ← **`T9`** إطفاء Cloudinary ← **`T6b`** الـmerge لـmain آخر شي بأمر خالد. بنود اللايف اليدوية (بند 66) بعد النشر. (**اكتمل على التست 2026-08-01: `T2b` تمليك 10/35/21 + idempotent · `T4` جولة السرعة الأولى (الإعادة = بند 67 يوم 08-05) · `T5` البروفة + جولة تفتيش 10 صفحات صفر مكسور صفر Cloudinary — الأدلة في تبويب Done بالـflow**) (الجروب الكامل + قاعدة الـcrash في T9). بالتوازي: مسار الأداء **`PERF-0→4 + C3`** · **Refactor الـUI/UX الكامل لمودونتي** — ملفه الوحيد `documents/tasks/MODONTY-UIUX-REFACTOR-v1.html` (فيه بنود «مدوّنتي ليس شريكاً» الأربعة المنقولة من خطة الـcore المقفلة). **ملاحظة dev مفتوحة:** revalidate الإعدادات يستهدف الإنتاج (`siteUrl` يمرَّر صراحةً في `updateAllSettings`) — اللوكال يحتاج ضربة تاغ يدوية. الدفع نفسه قرار خالد خارج القائمة. (المنجز كله موثّق في ملفات الـPRD/الـflow — ليس هنا.)

---

## 🔴 كبير — مشاريع + قرارات
- [ ] **74.** ⚠️ **قبل أي push: أرجِع `ARTICLES_TEST_LIMIT` إلى `null`** في `admin/app/(dashboard)/seo/components/cascade-status-panel.tsx:76` — الآن `10` لتسريع التست بأمر خالد. الشارة الصفراء «test mode» تظهر في الصفحة ما دام السقف مفعّلاً.
- [ ] **70.** 🧭 **صفحة `/seo` — متبقٍّ:** قلب ترتيب الأقسام ليتصدّر Full Rebuild · نقل الصفحة من مجموعة System المقفولة لمكان أظهر في الشريط الجانبي.
- [ ] **69.** 🖼️ **مراجعة الافتراضيات + ترخيص الصور والمحتوى (2026-08-02).** ✅ **نُفِّذ:** ضُغط **Apply Defaults** على التست فبُدِّلت **٥ قيم بالضبط** (تحقّقت من الجدول قيمة قيمة قبلها — الباقي كان مطابقاً): تعبئة `imageOwnerName` = «مدوّنتي» + `imageLicenseUrl` + `imageAcquireLicensePageUrl` = `https://www.modonty.com/legal/copyright-policy` (الصفحة موجودة وترجع 200 ومحتواها حقيقي) · تصحيح `orgAddressCountry` من «المملكة العربية السعودية» ← **`SA`** (رمز أيزو، هو المطلوب) · تصحيح `orgSearchUrlTemplate` من الآبكس ← **www**. **✅✅ فحص Google نهائي (2026-08-02):** صفحة `mbc-clinic` = **٤ أنواع كلها valid · صفر أخطاء · صفر تحذيرات**: Breadcrumbs 1 · **Image Metadata 3** · Local businesses 1 · Organization 1. **المكسب:** نوع `Image Metadata` **لم يكن موجوداً قبل الإصلاح** — ظهوره يعني أن صور مدوّنتي صارت مؤهّلة لشارة **Licensable** في بحث صور Google (ميزة ظهور إضافية، لا مجرد إزالة تحذير). **✅ تحقّق حيّ:** تبثّ `license` + `acquireLicensePage` على **اللوقو والغلاف** معاً، وخريطة الملكية تعمل كما صُمّمت: اللوقو `creditText = MBC clinic` (العميل) والغلاف `creditText = مدوّنتي` (إنتاجنا) — والترخيص من مدوّنتي في الحالتين. ملاحظتا Google انتهتا. **⚠️ ينطبق على الإنتاج أيضاً** (نفس الحقول فاضية هناك غالباً — يُتحقق قبل T8).
  - **✅ القراران حُسما (خالد 2026-08-02) ونُفّذا:** ① **رخصة المحتوى** — `defaultLicense` بُدّل من `CC BY 4.0` (كان يسمح بإعادة النشر **التجاري** بمجرد ذكر المصدر) إلى **رابط سياسة مدوّنتي** `https://www.modonty.com/legal/copyright-policy`، لأن نصّ السياسة يمنع الاستخدام التجاري وتدريب الذكاء الاصطناعي دون إذن مكتوب ← كان تناقضاً صريحاً. ② **`defaultNotranslate`** صار **`false`** — كان يمنع Google من عرض ترجمة الصفحات (موثّق رسمياً) ويناقض قاعدة CLAUDE.md العامة (notranslate لصفحات الدفع فقط). القيمتان مطبَّقتان في قاعدة التست + محدَّثتان في `seed-technical-defaults.ts` (ومصدر السطر في `system-form.tsx`).
  - **🔧 ترخيص صور المقالات — أُصلح 2026-08-02 (أمر خالد: «أي حاجة تخص السيو ما تتأجل»):** فحص العيّنات كشف أن صورة المقال فقط كانت مرخّصة (١/٥) — عُقد **المنظمة** تُبنى بمسار منفصل بلا ترخيص. الإصلاحات الثلاثة: ① `generateOrganizationNode` في `admin/lib/seo/knowledge-graph-generator.ts` صار يستقبل `imageLicensing` ويبني اللوقو/الصورة عبر `resolveImageAttribution` + `buildImageObject` (بدل كائن يدوي) ② `generatePlatformAuthorNode` لوقو مدوّنتي صار مرخّصاً ③ `generateSiteIdentityStructuredData` في `modonty/lib/seo/index.ts` صار يقبل رابطَي الترخيص + helper جديد `getPlatformImageLicensing()` في `get-platform-social-links.ts` يمرّرهما من صفحة المقال. **النتيجة الحيّة: ١/٥ ← ٤/٥** (الخامسة عقدة هوية الموقع — تُرندَر حيّة في مدوّنتي فتحتاج نشر preview للتأكيد). **tsc مدوّنتي + الأدمن: صفر أخطاء ✅** (شُغّلا بعد التعديلات مباشرة).
  - **✅ السقف المؤقت رُفِع (2026-08-02):** `ARTICLES_TEST_LIMIT = null` في `cascade-status-panel.tsx` — المرحلة رجعت ١١٨/١١٨ والشارة الصفراء تختفي تلقائياً. الثابت باقٍ كمقبض موثّق لأي تست سريع قادم.
  - **🟡 قيم ميتة مرصودة (بلا أثر — للتنظيف لاحقاً):** `defaultSitemapPriority`/`changeFreq` ونسختاهما للمقالات (Google: *"ignores `<priority>` and `<changefreq>`"* — وخريطتنا أصلاً ما تبثّهما) · `orgSearchUrlTemplate` (صندوق بحث الروابط أُزيل من نتائج Google، أكتوبر 2024) · `defaultOgImageType: image/webp` (توثيق Meta يذكر JPEG/PNG فقط — يُختبر بأداة المشاركة قبل أي تغيير).
  - **🖼️ بيانات:** **٣ عملاء بلا أي لوقو/صورة في قاعدة البيانات** (دكتور سمير شوقي · مركز فريق الإغاثة العربي · دكتورة سارة طارق) → عقدة المنظمة بلا `image`؛ **حلّها بيانات لا كود** (رفع لوقو من الأدمن).
- [ ] **68.** 🔄 **إكمال regenerate الشامل على التست — Articles (118) + Listings (1) قيد التشغيل الآن (2026-08-02 فجراً).** ⚠️ **درس مهم:** بعد أي تعديل على الإعدادات (مثل حقلَي الترخيص) **لا بد من إعادة توليد ثانية** — السيو مخبوز في قاعدة البيانات ولا يلتقط الإعدادات الجديدة إلا بإعادة التشغيل (العملاء أُعيد توليدهم مرتين لهذا السبب، والثانية أثبتت ظهور `license`+`acquireLicensePage` حياً). **قاعدة الفحص (خالد 2026-08-02): عشر عيّنات تكفي — لا تفحص الـ118 كلها.** المراحل الأربع الأولى اكتملت وتُحقّق منها بالدليل الخام + فحص Google: **Categories 14/14 ✅ · Tags 23/23 ✅ · Industries 7/7 ✅ · Clients 29/29 ✅** — كلها «1 valid item · صفر أخطاء/تحذيرات»، صفر Cloudinary، كل الصور `*.b-cdn.net` من مكتبة الوسائط، وتفريق YMYL مؤكَّد (MedicalClinic/Dentist/Hospital/Optician بـtelephone+priceRange · Organization نظيف بلا مطالبات). **التشغيل من `/seo`** بزر الـCascade الجديد (checkboxes + Start Selected + Cancel — بُني 2026-08-02 في `cascade-status-panel.tsx` بأمر خالد). التوليد **idempotent** فإعادة التشغيل آمنة. **⛔ على التست فقط** — الإنتاج محكوم بحارس T8.
- [ ] **67.** ⏰ **إعادة تست السرعة T4 كاملاً يوم 2026-08-05** (أمر خالد 2026-08-01 — بعد ٤ أيام يدفى كاش Bunny): الجولة الأولى الليلة أظهرت Cloudinary أسرع عند p75 (76ms مقابل 125ms دافئ / 430ms بارد — الكاش كان مصفّراً من أمس، HIT ارتفع 20%←84% خلال الجولات). السكربت جاهز: `scratchpad/t4-speed-test.mjs` (٤٠ صورة × ٣ جولات، قراءة فقط) — أعد بناءه لو انمسح الـscratchpad. القرار بعد الإعادة: لو الفرق ظل بحدود عشرات الميلي ثانية → مقبول ونكمل (قرار Brief #3 يُقفل ساعتها).
- [ ] **66.** 🔔 **بنود اللايف اليدوية — تتكرر على الإنتاج بعد الـSync (سُجّلت 2026-08-01 مساءً، كلها حُلّت على التست فقط):** ① الغلافان الميتان (التقويم الهجري + كأس العالم — فك ربط + حذف صفوف + الافتراضية تغطي) ② صورة وسم «خدمات طبية» ← مكتبة مدوّنتي (كانت صورة dream-to-app) ③ `Settings.ogImageUrl` ← مكتبة مدوّنتي (نفس الصورة) ④ ضبط `coreClientId` من /settings/system ⑤ T2b معاينة←LINK←إثبات idempotent. **التفصيل الكامل في كرت T5 بـ`BUNNY-GOLIVE-FLOW-v1.html`.**
- [ ] **65.** 🖼️ **fallback الصورة الافتراضية للمقال معطوب مرتين (كُشف 2026-08-01 بمقال «التقويم الهجري» الحي — الهيرو مكسور على الإنتاج):** الكود يعتمد بارام Cloudinary `d_article-placeholder-default` في `modonty/components/media/OptimizedImage.tsx:55` لكن (١) الأصل الافتراضي غير موجود على حساب Cloudinary أصلاً (مسجّل بسجل قديم: 404) و(٢) أي رابط مخزّن فيه `f_auto` يتخطّى الحقن كلياً (سطر 46) — فالفشل يظهر أيقونة مكسورة بدل الافتراضية. **الحل ضمن مسار Bunny:** fallback في الكود على أصل Bunny ثابت (نفس ما رصده بند ١٧ في `BUNNY-MIGRATION-PLAN-v1.html`)، ويشمل `fullOptmizeImage.tsx:293` (نفس النمط مكرّر). **✅ الشقّ الأول نُفّذ 2026-08-01 (أمر خالد):** helper مشترك `dataLayer/lib/platform-defaults.ts` يقرأ الافتراضيات الثلاث (صفوف PLATFORM عبر mediaSrc) + توصيلها في مدوّنتي: مقال بلا صورة بارزة → POST الافتراضية (`articles/[slug]/page.tsx` — جلب شرطي بلا كلفة على المسار الشائع) · عميل بلا شعار → LOGO الافتراضية · صفحة عميل بلا غلاف → HERO الافتراضية (`client-hero-v2.tsx` + prop في `page.tsx`/`client-page-shell.tsx`) — **تست حي ✓** مقال «الكلمات المفتاحية» (البلا-صورة الوحيد) يرندر الافتراضية. **المتبقي في البند:** كشف الملف المكسور وقت العرض (onError) + تنظيف بارام `d_article-placeholder-default` الميت من `OptimizedImage.tsx`/`fullOptmizeImage.tsx` + fallback في مولّدات السيو.
- [ ] **62.** 🔔 **قبل الـ go-live على الإنتاج (ترحيل Bunny):** تنزيل **أحدث لقطة يومية** للإنتاج (`.tar.gz`) + نسخة سكربت طازجة لحظة T-0. **✅ المفتاح جاهز (2026-08-01):** Atlas API Key أُنشئ واختُبر (فواتير ✅ · مشروع ✅ · قائمة لقطات Flex ✅ · تفويض التنزيل ✅) بصلاحيات Organization Billing Viewer + Project Owner + قائمة سماح مفتوحة (نصفا النطاق)؛ القيم الخمس محدّثة في `.env.shared` — **متبقٍّ على خالد:** تحديث `ATLAS_PUBLIC_KEY`/`ATLAS_PRIVATE_KEY` في Vercel Shared Env. المرجع: Flex = لقطة يومية تلقائية، آخر ٨ محفوظة، رابط التنزيل صالح ٤ ساعات.
- [ ] **64.** 📊 **تقرير Atlas في الأدمن (مالي + backup) — بعد اكتمال التاسك ٤ مباشرة:** إضافة كرت/قسم في `/database` يعرض الفواتير والتكلفة (`/orgs/{org}/invoices`) + قائمة لقطات الـ backup اليومية مع زر تنزيل (`/groups/{proj}/flexClusters/modonty-cluster/backup/snapshots` + `backup/download` — يحتاج `snapshotId`). البنية موجودة: `admin/lib/atlas/atlas-client.ts` يقرأ الـ env الخمسة الجاهزة. نفس نمط صفحة `/bunny` الجديدة (System ← Bunny، أُنشئت 2026-08-01).
- [ ] **10.** مراجعة سكيما شاملة + سياسة تكلفة مونجو (خالد 2026-07-24): جرد نمو الجداول · تداخل GA4 (دفع مزدوج) · فهارس TTL (`sessions`/`system_errors`/`audit_logs`) · فهارس زائدة · موديلات ميتة (`SeoIntake`/`JbrseoSubscriber`/`Subscriber` vs `NewsSubscriber`/`intake_*`) · حقول JSON منتفخة. المخرجات: تقرير جرد + سياسة احتفاظ + سكربت TTL (mongosh، إنتاج بإذن + باكب). **⚠️ بلا حذف حتى نراجع سوا** ([[feedback_check_datalayer_env]]).
- [ ] **11.** صلاحيات الأدمن لكل دور (الثغرة الحادّة انسدّت: `checkAdmin`+`proxy.ts` = طاقم نشط فقط). كل الأدوار تشوف كل شي، وعمليات الفلوس (`createInvoice`/`mark-paid`) بـ`auth()` لا role-gate. الخطة `ADMIN-PERMISSIONS-PLAN-v1.html` — خالد يقفل ٥ قرارات.
- [ ] **12.** «المهتمون» — قرارات + بناء: توحيد الموديلات الثلاثة (`NewsSubscriber`/`Subscriber`/`JbrseoSubscriber`) · حذف بوكس نشرة المقال · واجهة إطلاق الحملة بالأدمن · مفتاح الحملات في `notificationPreferences` + تشيك بوكس التسجيل (غير مؤشّر افتراضياً — GDPR/PDPL) + استعلام الاستهداف.
- [ ] **13.** GEO/AEO — التنفيذ (`GEO-AEO-AUDIT-2026-07-13.html`): سكور AEO (مرحلة ٢) · قياس AI referrals (classify-source + enum) · تقرير Bing · hreflang يُستبدل لا يُورَّث. (شُطبت جزئية «PII في /users/[id]» — الراوت غير موجود.)
- [ ] **14.** الريلز — الجهة الإدارية (مركونة): reels→GA4 · تست رفع الكونسول · موافقة الأدمن · ترحيل gallery القديمة · مرحلة الفيديو · قرار الكوتا. `documents/reels/`.
- [ ] **15.** تيليقرام: `leadHigh` **محجوب** (لا lead-scoring في مودونتي بعد) — `campaignInterest` صار مربوطاً. + مرآة الأدمن الكاملة + سجل أحداث موحّد. (22/26 يعمل.)
- [ ] **16.** معرض العميل — راوت مستقل + تخزين أوفر: راوت يعرض معرض كل عميل (إضافة+حذف) · شيله من `/media` (بلا كسر `/seo-images`) · نقله لـ Bunny.
- [ ] **17.** دفع بقية الدفعة الثانية: كونسول (gallery→Bunny يحتاج نموذج Reel) + مودونتي (geo tracking + booking_submit + reels) + نماذج Reels بالسكيما. 🔔 قبل أي دفع modonty: Lighthouse + GZIP + الباندل.

## 🔴 دمج الكيانات (Tag/Category/Industry) + تحويل 308
- [ ] **45.** **دمج/نقل كيان → آخر ثم حذف المصدر** (Tag→Tag · Category→Category · Industry→Industry). الخطورة: الروابط + السيو + JSON-LD.
  - [x] **خطوة 1 — آلية الـ308 (مبنية، tsc modonty صفر أخطاء):** موديل `Redirect{section,fromSlug,toSlug,@@unique}` · كاش قراءة fail-closed في `modonty/lib/archive-cache.ts` (`lookupRedirect`) · سطر في `modonty/proxy.ts` (يُفحص **بعد** isLive قبل الـ410) → `NextResponse.redirect(new URL(...), 308)`. **308 متحقّق من 4 مصادر رسمية:** RFC 7538 · MDN · Google Search Central (301≡308) · Next.js Docs. **بانتظار تست حي + push.**
  - [x] **خطوة 2 — فيتشر الدمج بالأدمن (Tag): مبني + تست حي محلي ناجح (2026-07-27) — بانتظار tsc + push.** `recordRedirect()` مع كسر السلاسل (`admin/lib/redirect/record-redirect.ts`) · محرك الدمج 3 أكشنات (`admin/app/(dashboard)/tags/actions/merge-tag-actions.ts`): `prepareTagMerge` (transaction: dedup على `@@unique[articleId,tagId]` + نقل + كتابة 308 + audit `tag.merge`) → `regenerateArticleSeoForMerge` (لكل مقال، idempotent) → `finalizeTagMerge` (SEO الوجهة+المصدر + listing + revalidate) + `getTagMergeImpact` (أرقام حقيقية للمعاينة) · UI: زر GitMerge بنفسجي + `tag-merge-dialog.tsx` — **عمودين** (يمين: المصدر←الوجهة + dropdown بحث · يسار: معاينة الأثر + بوابة كتابة اسم المصدر) + progress مقالاً مقالاً + شاشة اكتمال، المصدر يصير صفر ويحذفه خالد من الجدول. الموكاب المعتمد: `documents/tasks/merge-dialog-mockup-v1.html`.
    - **✅ تست حي محلي (modonty_dev، بورت 3000):** دمج `seo-tag-test` (٦ مقالات) → `SEO` الحقيقي: نُقلت ٦ (١٢←١٨) · ٠ مكرّر · أُعيد توليد SEO+JSON-LD لـ٦ · **308 اتسجّل** · المصدر صار ٠ مقال. الدائرة كاملة تعمل.
    - **حراسة الجدول:** زر Delete مقفول ما دام `count>0` (لازم دمج أولاً) · الوسم بصفر مقال = بادج amber «0 · Empty» (لون مميّز، معيار الأدمن).
    - **✅ تحقّق على مستوى المقال (2026-07-27):** دمج «أدوات»(1)→«Google» → محرّر المقال `6a60a5dd` يعرض وسومه = Google·SEO·محتوى (أدوات اختفى، Google أُضيف). أدوات صار Empty · 308 `tools-tag-test→google-tag-test`. **الدمج مؤكّد 100%.**
    - **✅ عدّاد الوسم — وُحِّد (2026-07-27، مو كاش):** كان التضارب (قائمة=5 مقابل تفاصيل=4) لأن `tag-view.tsx` يعدّ **المنشور فقط** (`publishedArticlesCount`) بينما القائمة تعدّ **كل** الروابط (`_count.articles`). أُصلح: التفاصيل تعرض `totalArticlesCount` (الكل، مطابق للقائمة + بوابة الحذف/الدمج) + «(N published)» ثانوي. **الدمج نفسه كان dedup صحيح** (المقال كان أصلاً موسوماً بالوجهة → اتحذف رابط المصدر المكرّر، صفر فقدان).
    - **⚠️ قبل التست/الدمج على الإنتاج:** `prisma db push` لإنشاء مجموعة `redirects` + فهرس `@@unique([section,fromSlug])` على الإنتاج (مجموعة جديدة فارغة، بلا backfill؛ باكب أولاً).
  - [x] **خطوة 3أ — تعميم على Category: مبني + تست حي محلي ناجح 100% (2026-07-27) — بانتظار push.** `merge-category-actions.ts` (getCategoryMergeImpact · prepareCategoryMerge [transaction: `article.updateMany` categoryId مصدر→وجهة **بلا dedup** لأن التصنيف مفرد + `category.updateMany` parentId لإعادة ربط الأبناء + 308 «categories» + audit `category.merge` + **حجب الدمج في حفيد** لمنع دورات الهرمية] · finalizeCategoryMerge) · يعيد استخدام `regenerateArticleSeoForMerge` (JSON-LD المقال يحمل `articleSection`=اسم التصنيف) · `category-merge-dialog.tsx` (نفس ديالوج الوسم + سطر «تصنيف فرعي يُعاد ربطه») · قفل حذف على المقالات **أو** الأبناء (`get-categories` أضاف `_count.children`) · amber «0 · Empty». **تست حي:** العقارات(1)→السيو SEO: المقال «شركات مقاولات» تصنيفه صار السيو SEO [selected] · العقارات=0 · السيو=15→16 · 308 مُسجّل. **ملاحظة:** إعادة ربط الأبناء مبنية+محمية لكن ما تفعّلت حياً (لا مصدر تجريبي عنده أبناء). عدّاد التصنيف نظيف أصلاً (بلا إصلاح مثل الوسم).
    - [ ] **⏳ تست معلّق (أب/ابن في دمج التصنيف الواحد):** إعادة ربط الأبناء (`parentId` مصدر→وجهة) مبنية ومحمية لكن **ما اختُبرت حياً**. لاحقاً: أنشئ تصنيفاً فرعياً تحت مصدر ثم ادمج المصدر → تأكّد أن الابن صار تحت الوجهة، والمصدر انفرغ من الأبناء وصار قابلاً للحذف، وأن الابن ما يحتاج إعادة توليد سيو (breadcrumb مسطّح، مؤكَّد).
  - [x] **خطوة 3ب — تعميم على Industry: مبني + تست حي محلي ناجح 100% (2026-07-27) — بانتظار push.** `merge-industry-actions.ts` (getIndustryMergeImpact · prepareIndustryMerge [transaction: `client.updateMany` industryId مصدر→وجهة **بلا dedup** + 308 «industries» + audit `industry.merge`؛ **بلا أبناء** — الصناعة مسطّحة] · `regenerateClientSeoForMerge` [يعيد توليد سيو **العميل** عبر `generateClientSeoBundle` المشترك — knowsAbout يحمل اسم الصناعة] · finalizeIndustryMerge) · `industry-merge-dialog.tsx` (بصيغة «عميل») · قفل حذف على العملاء · amber. **حاسم:** اسم الصناعة يدخل JSON-LD **العميل فقط** (knowsAbout)، و**لا يظهر في Organization node داخل المقال** → **صفر تشعّب لمقالات العميل** (تُحُقّق من `knowledge-graph-generator.ts:507-574`). **تست حي:** العقارات والتطوير(1)→التجارة الإلكترونية: عميل «شركة جبر الجنوبية» صناعته صارت التجارة الإلكترونية [محرّر العميل] · العقارات=0 · التجارة=2→3 · 308 مُسجّل.

## 🟢 الباك لوق — مؤجّل (مشاريع/قرارات/أفكار)
- [ ] **63.** أتمتة أرشيف باكب طويل المدى: سكربت مجدول يسحب لقطة Atlas عبر API ويرفعها على storage zone **خاص** في Bunny (احتفاظ شهري) — يسد فجوة الـ8 أيام في لقطات Flex. ممنوع Git (بيانات مستخدمين). بعد اكتمال ترحيل Bunny فقط. **📌 المبدأ المعماري (خالد 2026-08-01): Bunny = بيت الملفات كله** (clients + assets + reels + أرشيف الباكب لاحقاً) · **Atlas = بيت البيانات** (مقالات/مستخدمين/إعدادات) — مزوّدان فقط بعد إطفاء Cloudinary، كلٌّ في تخصصه.
- [ ] **46.** **شرح CrUX الكامل في الـ Guideline (skill `image-speed`):** أضِف قسماً يشرح لماذا GA4 يُظهر زواراً بينما CrUX يُظهر «No Data». **الزائر المؤهّل لـ CrUX = ٤ شروط مجتمعة:** (١) متصفح Chrome فقط لا سفاري/فايرفوكس، (٢) مُفعّل «إرسال إحصائيات الاستخدام»، (٣) مسجّل دخول ومزامنة سجل التصفح شغّالة، (٤) بدون كلمة مرور تشفير للمزامنة + منصة مدعومة (ديسكتوب/أندرويد Chrome). **زيادةً:** الصفحة لازم تتجاوز «حدّ الشعبية» (عدد أدنى غير معلن من الزيارات المؤهّلة). الخلاصة: GA4 يعدّ كل زائر عبر سكربت الصفحة، CrUX يعدّ فقط شريحة Chrome المُطوَّعة فوق الحد → لا عقوبة أداء فعلية من Google ما دام CrUX «No Data». المصدر الرسمي: `developer.chrome.com/docs/crux/methodology`.
- [ ] **18.** جلسة فريق المحتوى — 7/11 قرار مفتوح. `CONTENT-TEAM-BRAINSTORM-v1.html`.
- [ ] **19.** نظام الروابط المختصرة — 9 مراحل + 3 قرارات (git: `SHORT-LINK-SYSTEM-TODO.md`). **يشمل QR لكل عميل** (QR = تمثيل بصري لرابط مختصر، نفس البنية التحتية).
- [ ] **20.** جرد الإيميلات الشامل — كل trigger/قالب عبر الثلاثة + Resend (حوافز النشرة الست «قريباً»).
- [ ] **21.** مُراجِع YMYL — الطبيب الموافق بالكونسول = المُراجِع (`reviewedById`). المؤجل: MEDICAL-YMYL (~32س). `YMYL-FLOW-DISCUSSION.md`.
- [ ] **22.** صيانة DB داخل Run-All (تنفيذ decay): Analytics/ArticleView (365ي) · إشعارات (30ي) · articleBodyText · NewsSubscriber ملغي (6ش) · Conversion/CampaignTracking · العدادات · يتيمة. **⚠️ ينفّذ ضمن سياسة الاحتفاظ الموحّدة التي يقرّرها البند ١٠ (لا سياستين متضاربتين).**
- [ ] **23.** إعادة تصميم بوابة جودة المقال — قائمة الكاش تكذب + خلط «محسوب» و«ناقص».
- [x] **55.** ~~مسح مدوّنتي الكامل (`C1`) — كل صورة مرندرة~~ — ✅ نُفِّذ 2026-07-30. **النطاق:** ١٧٤ راوتاً = كل رابط في `sitemap.xml` بلا عيّنات (١٥ تصنيفاً · ٣٠ عميلاً · ٢٤ وسماً · ٨ صناعات · ٧٥ مقالاً) + الثابتة؛ صفر فشل. **النتيجة:** **٢٦٠٢ صورة `<img>` مرندرة · ٢١٥٤ Bunny (٨٢٫٨٪) · ٤٤٨ Cloudinary (١٧٫٢٪)** · JSON-LD ٩٣ Bunny مقابل **١١٥٢ Cloudinary** · ١٣٢ من ١٧٤ راوتاً فيها أثر واحد على الأقل. **✅ الكود نظيف — ثلاثة فحوص:** (١) ماسح آلي يمشي على الأقواس فحص ١٤٥ ملفاً فيه `select` ← **صفر select ناقص `bunnyUrl`** (صنف السقوط الصامت الذي ضرب الأدمن **غير موجود** في مدوّنتي) (٢) صفر كاست يمحي `bunnyUrl` (٣) صفر رابط Cloudinary مكتوب في الكود بعد إصلاح `/story`. **السبب الجذري = بيانات:** افتراضيات المنصّة (`modontyAvatar` + `article-placeholder`) هي صفوف `Media` بـ`scope=PLATFORM` و`bunnyUrl=null` — والكود يقرأها بـ`select { url, bunnyUrl }` + `mediaSrc()` سليماً؛ الصفّ هو الناقص. والباقي صور عملاء/مقالات + `Settings.ogImageUrl` + `socialImage` القوائم + سيو مخزّن — **كلّها ما يعالجه البند 50**. **المنهجية:** `DOMParser` ← كل `<img src>` ← فكّ `/_next/image?url=` للأصل + فحص JSON-LD منفصل. **تنظيف مؤجّل لـ E2:** `app/layout.tsx:78` dns-prefetch لـ Cloudinary + تعليق JSDoc في `fullOptmizeImage.tsx:38`. **⚠️ البوابة لا تُقفل** حتى يُشغَّل البند 50 وتُعاد بصفر.
- [x] **56.** ~~`ClientReview.author` اسم مضلّل + أيتام تُسقط توليد السيو~~ — ✅ عولج 2026-07-30. **الاسم:** `author`/`authorId` → `reviewer`/`reviewerId` مع **`@map("authorId")`** فالحقل المخزّن في مونجو **لم يتغيّر** — أُثبت بقراءة خام: حقول الوثيقة `_id · clientId · authorId · rating …` و`reviewerId` غير موجود. **صفر ترحيل بيانات، صفر أثر على الإنتاج.** السبب: `reviewer` هو `User` (زائر) لا موديل `Author` (كاتب المقال) — اسم واحد لمعنيين. **الأيتام:** من وثائق Prisma حرفياً «MongoDB doesn't natively support referential actions… Prisma polyfills… on a best-effort basis» — أي `onDelete: Cascade` **وعد بلا ضامن** يعمل فقط لحذف يمرّ عبر Prisma. جرد كامل: **٥٣ علاقة إجبارية**، وفيها **٣ صفوف يتيمة فقط** (`clientReview.authorId`×٢ · `articleTag.tagId`×١) كانت تُسقط **١١ عميلاً ومقالاً**. **العلاج البنيوي:** القراءة تختار الحقل المجرّد وتحلّ الاسم منفصلاً بدل الجوين على علاقة إجبارية — طُبّق في `dataLayer/lib/seo/generate-client-seo-bundle.ts` و`console/.../client-review-queries.ts` (مدوّنتي كانت مطبّقاه أصلاً، فالثلاثة متسقة الآن).
- [x] **50.** ~~كرت ترحيل مستقل بزرّ واحد (Cloudinary ← Bunny)~~ — ✅ بُني وشُغِّل 2026-07-30. **النتيجة: ٣٧٦ ← ٦** على `modonty_dev` · الحقول الخام **صفر** · صفر خطأ prisma في ٢٠ دفعة. **البنية:** `database/actions/cloudinary-to-bunny.ts` + `cloudinary-scopes.ts` + `components/cloudinary-migration-card.tsx` — **١٠ نطاقات** بترتيب مفروض في الكود (وسائط ← روابط معلّقة ← يتيمة ← خام ← مقالات ← عملاء ← تصنيفات ← وسوم ← صناعات ← قوائم)، **خارج Run-All**. **قرار معماري:** المتصفّح يقود الحلقة بدفعات من ٥ بدل server action واحد — الأول كان يرجع بعد ١٢ دقيقة وهو ذرّي فلا نسبة ولا إيقاف؛ الآن نسبة حقيقية + **زر Cancel** يسري عند حدّ الدفعة والمنجَز يُحفظ. **٣ باقات «سقوط صامت» أُصلحت في الطريق:** `knowledge-graph-generator.ts:495` (`item.media.url` لمعرض المقال — ٩٦ مقالاً كانت تعيد خبز Cloudinary مهما أعدت التوليد) · `generate-client-seo-bundle.ts` (select معرض العميل بلا `bunnyUrl` — ١٠ عملاء) · `console/gallery/page.tsx` + `admin/get-client-media.ts`. **ماسح ثانٍ** بُني للصنف الذي فات الأول (استعلام `db.media` المباشر) — مسح ٢١٩١ ملفاً. **المتبقّي ٦ ليس مشكلة ترحيل:** صورتا مقالين **محذوفتان من Cloudinary (HTTP 404)** فلا أصل يُنسخ — تُحلّان برفع غلاف جديد من الأدمن (يذهب لـ Bunny مباشرة).
- [x] **57.** ~~شريط تقدّم مشترك للمهام الطويلة~~ — ✅ بُني 2026-07-30 بطلب خالد («spinner وحده ما يقول إذا كان يشتغل ولا عالق»). `admin/components/admin/task-progress.tsx` — وضعان: **نسبة حقيقية** (`current`/`total`) أو **تقديرية** تزحف لـ٩٥٪ وتقف فلا تدّعي الاكتمال قبل رجوع المهمة، مع ساعة من الوقت الحقيقي وسطر يوضّح أن التقدير تقدير. صالح لأي مهمة طويلة قادمة. — عملية لمرّة واحدة، **ما تدخل Run-All**. قائمة عمله مقاسة على قاعدة التست: **٢ صفوف وسائط** بلا نسخة على Bunny + **٣ مقالات** بروابط محفورة في المحتوى + **١١٢ مقالاً** سيوها المخزّن يشير Cloudinary (٤٢٢ صفّاً آمن أصلاً بنسخة Bunny). لمّا تصير أصفاراً على الإنتاج = الانسحاب اكتمل. التفاصيل `D0` + `D0-a` في `BUNNY-GOLIVE-PRD-v1.html`. — قرار خالد 2026-07-30: عملية لمرّة واحدة، **ما تدخل Run-All** (ذاك للصيانة المتكرّرة). يجمع: (١) روابط صور الإعدادات الستّة الخام `logoUrl`/`logoIconUrl`/`ogImageUrl`/`categoriesPageImage`/`tagsPageImage`/`industriesPageImage` (٢) روابط Cloudinary المحفورة داخل `content` HTML (٣) إعادة توليد السيو المخزّن آخر خطوة. قابل لإعادة التشغيل بأمان + «فحص فقط» قبل التنفيذ. التفاصيل `D0` في `BUNNY-GOLIVE-PRD-v1.html`.
- [x] **51.** ~~`/articles/workflow/maintenance` — ٢٥ صورة Cloudinary مرندَرة + بقيّة تسريبات الـ select~~ — ✅ عولج 2026-07-30. الصفحة صارت **٠ Cloudinary / ٢٦ Bunny**. المسح المنهجي (سكربت يمشي على الأقواس ويصطاد كل select لعلاقة وسائط بلا `bunnyUrl`) رصد **٣١ موقعاً**؛ عولج **٢٦** والباقي ٥ في `/social/facebook` (تابعة للبند **49**). عولج كذلك صنف الـ**سقوط الصامت**: نداء `mediaSrc()` على select ناقص `bunnyUrl` كان يرجّع Cloudinary دائماً — أخطر ما وُجد، وضرب مولّدات السيو المخزّن (`knowledge-graph-generator` · `metadata-storage` · `generate-client-seo-bundle` · `generate-organization-jsonld` · `client-jsonld-storage` · `listing-page-seo-generator` · بناة JSON-LD للرئيسية/الرائج/العملاء). و**كسر إخفاء بالكاست**: `client-form.tsx` كان يكتب `as { url?: string }` فيمحي `bunnyUrl` من النوع → صفحة `/clients/[id]/edit` ترندر شعار+غلاف Cloudinary (٢ لكل عميل، تأكّد على ٣ عملاء). **تست حيّ نهائي: ٧٤ راوت أدمن · ٣٨٠ صورة `<img>` مرندرة · ٣٠٧ Bunny · ٧٣ Cloudinary كلّها على `/social/facebook` وحده.** tsc صفر أخطاء على الثلاثة.
- [ ] **54.** **تأسيس عميل «مدونتي» core** — مشروع مستقل، أُخرِج من ملف ترحيل Bunny 2026-07-30 (قرار خالد: «فيه لها شغل كتير وأفكار كتيرة»). **الفكرة:** حالياً «modonty» مجرّد كلمة سحرية في الرفع (`clientId="modonty"` → `scope=PLATFORM`، `clientId=null`). نرقّيها لعميل حقيقي → أصول المنصّة تدخل media library زي أي عميل → تاخذ `bunnyUrl` تلقائياً → تُغني عن معالجة الحقول الخام [S] يدوياً. **الخطوات الثلاث (نفس بنية الملف):** **(A1)** إنشاء/تحديد عميل `Client` بـ slug ثابت (مثلاً `modonty`) يمثّل المنصّة — قرار: عميل جديد أو ربط موجود؛ القبول = عميل بـ ID ثابت معروف. **(A2)** حقل `coreClientId` في `Settings` يشير لعميل A1 (`dataLayer/prisma/schema` + admin settings action/UI) عشان النظام يعرف مين الـ core ويقرأه بدل الكلمة السحرية. **(A3 — قلب الحل)** توجيه أصول المنصّة لمكتبة العميل في folder خاص، والحقول [S] هي: `brand.ts LOGO_URL` · `Settings` (`logoUrl`/`ogImage`/صور التصنيفات) · `Author.image` · `User.image`/`avatar` · `socialImage` على (Article · Category · Tag · Industry · Client) — كل أصل يُرفع عبر المكتبة فيأخذ `bunnyUrl`، والقرّاء يقرؤون من المكتبة بدل الرابط اليدوي؛ القبول = صفر رابط Cloudinary يدوي في الكود/الإعدادات. **✅ المُنجَز منها سلفاً بطريق أبسط:** أصول العلامة الأربعة على Bunny عبر `dataLayer/lib/brand-assets.ts` + حقول الإعدادات الستّة الخام رُحّلت — فالمشروع **لم يعد يحجب ترحيل Bunny** إطلاقاً. **⚠️ ما زال مطلوباً لـ:** `Author.image` · `User.avatar` · `socialImage` للكيانات الخمسة. التوثيق الأصلي محفوظ في بطاقة `A1-A3` في `BUNNY-GOLIVE-PRD-v1.html`.
- [x] **59.** ~~`bunnyUrl` اختياري في `MediaSrcInput` = تسريب صامت لا يمسكه `tsc`~~ — ✅ عولج 2026-07-30/31 بقرار خالد («نبغى نتيجة 100% سليمة»). **الجذر:** `MediaSrcInput.bunnyUrl?` كان **اختيارياً**، فأي شكل مضيَّق (`{ url: string }`) يبقى وسيطاً صالحاً — يعني `select` نسي الحقل، أو `.map()` أسقطه، أو نوع prop ضيّقه، كلها **تُترجم نظيفة وتخدم Cloudinary للأبد**. **العلاج البنيوي:** جُعل الحقل **إجبارياً** (قيمته قد تبقى `null`)، فتحوّل كل تسريب صامت إلى **خطأ `tsc` عند موقع النداء**. **الحصيلة: ٩٢ موضعاً كشفها المترجم** — مدوّنتي ٣٥ · الأدمن ٥٢ · الكونسول ٥ — **كلها أُصلحت، والتطبيقات الثلاثة `tsc` صفر أخطاء.** **أبرز ما كُشف:** `article-featured-image.tsx` (صورة الـLCP في كل مقال!) · `client-gallery-section.tsx` (`src={img.url}` بلا `mediaSrc` — مصدر ١٠٧ صور على صفحات العملاء) · `sitemap.ts` + `image-sitemap.xml` (نوع يمحو الحقل) · `GalleryItem` + `form-types` + `ArticleViewType` + `ClientGalleryImage` وأنواع مشتركة أخرى. **استثناء موثّق:** `ArticleViewType.featuredImage` تُرك `bunnyUrl?` اختيارياً بتعليق يشرح السبب — استعلام قائمة المقالات يجيب `{id, altText}` فقط ولا يرندر صورة. **درس:** قدّرتُ ٣٤ موضعاً فطلعوا ٩٢؛ ومسح آلي واسع على الأدمن أنشأ تكرارات رفعت الأخطاء ١٢→٢١ قبل التنظيف — **التعديل الآلي على الأنواع يحتاج تحقّقاً بعد كل دفعة لا في النهاية**.
- [x] **58.** ~~`B3` اتساق sitemap الصور + OG مع ما تعرضه الصفحة~~ — ✅ **15/15 صفحة متسقة، صفر Cloudinary مرندر** (2026-07-30). فاحص يقارن ثلاثة مصادر لكل صفحة: `image-sitemap.xml` ← `og:image`/`twitter:image` ← الصور المرندرة (بفكّ `/_next/image?url=`). **المعيار نفسه كان ناجحاً من البداية** (sitemap=OG=Bunny) لكن الصفحة كانت ترندر ٣–٩ صور Cloudinary — تضارب أخطر، لأن جوجل يرى مضيفاً في الوسم وآخر في الترميز. **ثلاث تسريبات كشفها الفحص:** (١) `articles/[slug]/page.tsx:284` تحويل المعرض يقرأ `g.media?.url` — الـselect فيه `bunnyUrl` والمكوّن ينادي `mediaSrc` **لكن التحويل بينهما محا الحقل** (٢) `article-featured-image.tsx` نوع الـprop `{url, altText}` يمحو `bunnyUrl` والمكوّن يقرأ `image.url` مباشرة — **وهذي صورة الـLCP في كل مقال** (٣) ٦ مواضع أخرى (معارض الأدمن ×٣ · وسائط الكونسول ×٣). **صنف جديد مسجَّل — «مَحْو النوع»:** الـselect سليم والمكوّن ينادي `mediaSrc`، لكن نوعاً وسيطاً يضيّق الكائن إلى `{url}` فيسقط `bunnyUrl` قبل وصوله؛ `tsc` لا يشتكي لأن النوع المضيَّق مجموعة جزئية صالحة. **ماسحان جديدان** بُنيا (تحويلات + محو النوع) ورصدا **٤٣ موضعاً** يستحقّ المراجعة — الباقي منها لم يُثبت تسريبه حيّاً بعد.
- [x] **53.** ~~صفحة `/story` — روابط Cloudinary مكتوبة في الكود~~ — ✅ عولج 2026-07-30. **الرقم الصحيح ١٨ لا ٢١** (الثلاثة الباقية في `bunny-test/pairs.ts` مقصودة للمقارنة). **الشعار:** حُمِّل وقُورن بايت ببايت مع `brand/modonty-wordmark.svg` على Bunny → **متطابقان** (`sha 0057be46…` · ٥٧٢٨ بايت)، فأعيد استخدام `BRAND_WORDMARK_URL` بدل رفع نسخة مكرّرة — وبها انحلّت `STORY_OG_IMAGE` (صورة المشاركة). **الأصول الـ١٧** (١٢ فريق + ٥ شركاء) رُفعت إلى `assets/brand/story/` بأسماء دلالية (`team/khalid.jpg` · `partners/kima-zon.jpg` · `partners/alsaha-tech.jpg` …) بدل عشوائيات Cloudinary. أُضيفت مساعدتان في `_constants.ts` — `storyTeamImage()` + `storyPartnerImage()` — نقطة تبديل واحدة. الحقل `cloudinaryUrl` في `TeamCarousel` أُعيد تسميته `imageUrl`. **الدليل:** ١٧/١٧ مُتحقَّقة على `storage.bunnycdn.com` بالـ`AccessKey` (لا `b-cdn.net` — يكذب على المحذوف) · ١٧/١٧ + الشعار تُخدَم **٢٠٠** من الـCDN · `grep res.cloudinary.com modonty/app/story/` ← **صفر** · `tsc` مدوّنتي صفر أخطاء. **⚠️ غير مُتحقَّق حيّاً** — الصفحة ما انفتحت في متصفّح؛ يُغطّى في `C1`.
- [x] **A0.** ~~شعار مدوّنتي وأفاتاره مكتوبان في الكود على Cloudinary~~ — ✅ عولج 2026-07-30 بملف ثوابت مشترك واحد `dataLayer/lib/brand-assets.ts` فيه ٤ ثوابت كلّها على `modonty-asset.b-cdn.net/brand/` (شعار PNG · أفاتار WebP · ووردمارك SVG · أيقونة SVG)، تستوردها التطبيقات الثلاثة — صفر تكرار وصفر Cloudinary في JSON-LD (`publisher.logo`) وشريط التنقّل وقوالب البريد. **ملاحظة:** الحل جا أبسط من المقترح (ملف ثوابت بدل «عميل core + مكتبة»)، فبنود `A1`/`A2` (`coreClientId`) **لم تعد ضرورية** — تحقّقت: الحقل غير موجود في السكيما ولا في الكود.
- [x] **49.** ~~مراجعة كاملة لنشر السوشال (فيسبوك/إنستغرام)~~ — ✅ **أُلغيت الميزة كلها 2026-07-30** (قرار خالد: «خاصية ما لها داعي، كود على الفاضي — الغيها تمامًا بالـ Route، شيلها من الـ Sidebar، شيلها من الأدمن تمامًا»). **المحذوف:** مجلّد `admin/app/(dashboard)/social/` كامل — ٦ ملفات (`facebook/page.tsx` · `facebook/[articleId]/page.tsx` + مكوّنه · `facebook/_actions.ts` · `facebook/_components/facebook-page-client.tsx` · `instagram/page.tsx`) · بند «Social Media» من `components/admin/sidebar.tsx` · مدخل `/social` من `NON_NAVIGABLE_PATHS` في `breadcrumb-utils.ts`. **أُبقي عمداً (مُتحقَّق أنه مستقل):** `lib/utils/sharp-loader.ts` (مشترك مع قصّات الوسائط) · لابل `social: 'Social'` في breadcrumb (تخدم `/settings/social` الحيّة) · `getSocialPlatform` في مدوّنتي (أيقونات روابط العميل). **بذلك سقط بلوكر `sharp` وشقّ الصور معاً** والأدمن صفر Cloudinary كوداً. `tsc` صفر خطأ مصدري. **✅ والنشر التلقائي أيضاً حُذف** (إذن خالد «remove» في نفس الجلسة): `admin/lib/social/meta.ts` + `twitter.ts` · كتلة النداء في `update-article.ts` (`isFirstPublish` + `socialParams` + الاستيرادان الديناميكيان) · الاستيراد اليتيم `mediaSrc` من نفس الملف · ومن السكيما: موديل `SocialPost` + `enum SocialPlatform` + `enum SocialPostStatus` + حقل العلاقة `socialPosts` على `Article`. **مجموع المحذوف: ٨ ملفات + كتلة كود + ٣ كيانات سكيما.** الطقس الإلزامي طُبّق: `taskkill node` ← `prisma:validate` ✓ ← `prisma:generate` ✓ ← مسح `admin/.next` ← **`tsc` صفر أخطاء على التطبيقات الثلاثة**. **⚠️ مجموعة `social_posts` في مونجو لم تُسقَط** — Prisma فقط توقّف عن ربطها؛ الصفوف القديمة تبقى مقروءة بـ mongosh. إسقاطها يدوياً (بعد باكب) متى ما تأكّدنا أنها غير مطلوبة. **أُبقي عمداً:** `sharp-loader` · لابل `social` في breadcrumb (لـ`/settings/social`) · `getSocialPlatform` في مدوّنتي.
<!-- سِجل البند 49 السابق (للمرجع): كان «فيه مشاكل من البداية»، آخر تاسك في مسار Bunny. التحويل لـ Bunny مكتوب و tsc نظيف في `admin/app/(dashboard)/social/facebook/_actions.ts`، **لكن غير مُتحقَّق حيّاً**: «Preview Default Image» يفشل بـ `ERR_DLOPEN_FAILED` على sharp داخل سيرفر Next بينما `require("sharp")` ينجح في node عادي (بعد مسح `.next` و٣ إعادات تشغيل). المراجعة تشمل: مسار الصور · بيانات Meta · حالات الفشل · الفرق عن مسار قصّات الوسائط الذي يعمل بنفس المُحمِّل. **دليل جديد (فحص حيّ 2026-07-30):** `/social/facebook` يرندر **٧٣ صورة `<img>` من Cloudinary** مقابل ٢ فقط من Bunny — أي مسار العرض هناك لم يمرّ على `mediaSrc()` بعد. بعد إغلاق البند **51** هذه هي **آخر بقعة Cloudinary في الأدمن كلّه** (٧٤ راوتاً مفحوصاً، البقيّة صفر). **✅ شقّ الصور أُقفل 2026-07-30** (قرار خالد: «خلّص ما يخصّ الصور، تقرأ من Bunny» — والمراجعة نفسها تبقى مفتوحة): المواقع الخمسة عولجت **بالنصفين معاً** (`bunnyUrl: true` في الـ`select` **و** التمرير عبر `mediaSrc()`) — `social/facebook/page.tsx:34,78` ← الاستهلاك `:92,105` · `[articleId]/page.tsx:30` ← `:62` · `_actions.ts:87,274` (متغيّر `coverUrl` يُمرَّر لـ`generateInstagramDefaultImage`). الدليل: `grep "featuredImage.url"` على `social/` ← **صفر**، وكل `select` فيه `bunnyUrl`، و`tsc` الأدمن صفر أخطاء. **بذلك الأدمن صفر Cloudinary كوداً على ٧٤ راوتاً.** **الباقي في البند:** بلوكر `sharp` + بيانات Meta + حالات الفشل + الفرق عن مسار قصّات الوسائط — **مستقلّ عن الصور ولا يحجب الانتقال لمدوّنتي**؛ التست الحيّ لصور السوشال مرهون بحلّ `sharp`. -->

- [x] **52.** ~~صفحة Bing Webmaster — «أفضل الصفحات» تنهار وجدول الكلمات يكذب~~ — ✅ عولج 2026-07-30 بدليل من **الـ API الحيّ** (١٤٩ صفّ صفحات + ٢٧١ صفّ كلمات، مو تخمين). ثلاثة أخطاء لا واحد: **١)** `GetPageStats` يرجّع كائنات `QueryStats` (الـ `__type` يشهد) فرابط الصفحة يجي في حقل `Query` و**حقل `Page` غير موجود أصلاً** → `p.Page.replace()` يرمي `TypeError`، وكل الـ١٤٩ صفّاً كانت تُرمى. **٢)** الردّ **صفٌّ لكل يوم** لا مجاميع (١٤٩ صفّاً = ٥٧ رابطاً مميّزاً؛ الرئيسية وحدها ١٠ صفوف) → الترتيب كان يرتّب **أياماً منفردة**، فجدول «أفضل ١٠» يكرّر نفس الرابط/الكلمة (جدول الكلمات كان «يعمل» وهو يكذب: «جبر سيو» ٧ مرات). **٣)** `AvgClickPosition: -1` علامة «صفر نقرات» لا ترتيب. **الحل:** `aggregateBingStats` في `lib/bing-webmaster/client.ts` يجمع حسب المفتاح (نقرات + ظهور، ومتوسّط ترتيب **مُرجَّح بالظهور**) قبل الترتيب · `BingPageStat` صار نوعاً واحداً مع `BingQueryStat` (نفس شكل السلك) · `safeDecodePath` يحمي من `URIError` على `%` معطوب. **تحقّق مستقل:** «جبر سيو» = ٠ نقرة / ٨٤ ظهوراً / ترتيب 6.2 والرئيسية = ٤٧ / ١٢١ — طابقت الحساب من الـ API الخام حرفياً. الجدولان الآن ١٠ صفوف بصفر تكرار، وصفر أخطاء في لوق السيرفر (قبلها كان يسجّل `TypeError` كل رندر).
- [x] **48.** ~~صورة الزائر في مدوّنتي تُخزَّن base64 داخل القاعدة~~ — ✅ عولج 2026-07-30: راوت `modonty/app/api/users/avatar/route.ts` يرفع إلى Bunny (`avatars/users/`) · السكيما ترفض روابط `data:` · تحقّق Zod على الخادم داخل `updateProfile` · تست حي: القاعدة تخزّن رابطاً بـ ٨٨ حرفاً (`isBase64:false · isBunny:true`).
- [ ] **47.** **تأكيد موافقة العميل في الكونسول = إشعار عائم عمره ٨ ثوانٍ** (`console/app/(dashboard)/dashboard/articles/components/article-preview-client.tsx` → `handleApprove` يستدعي `toast()` بزرّ «نعم، وافق»). لو تأخّر العميل يضيع التأكيد بلا رسالة ولا أثر، والزرّ يبدو كأنه لا يعمل — ضربني فعلياً في تست 2026-07-30. **الحل:** مربّع حوار (AlertDialog) مثل الأدمن. قرار موافقة لا يُبنى على مؤقّت.
- [ ] **24.** Hero 1200×630: رفع القديمة 6:1 + كروبر الكونسول + `hero-cover.tsx` (3:1→6:1).
- [ ] **25.** روابط داخلية سياقية داخل جسم المقال (سيو داخلي).
- [ ] **27.** مراجعة خبير لكلمات الرئيسية (homepage) بعد النشر.
- [ ] **28.** Google Reviews للـ Premium — Places API (بلا AggregateRating + بلا كاش).
- [ ] **29.** نظام Reviews لمودونتي (آراء العملاء على modonty.com).
- [ ] **30.** تدقيق فاتورة Vercel — `vc> audit`.
- [ ] **31.** بطاقة عروض الشركاء — MediaType OFFER + قرار validUntil.
- [ ] **34.** إعادة تصميم صفحة العميل بالأدمن + intake للقراءة + نشاط العميل.
- [ ] **37.** قسم خدمات العميل — موديل ClientService.
- [ ] **38.** تدقيقات تنظيف JSON-LD: حذف `Client.keywords` (~33 موقع) · بطاقة ClientKeyword بالمحرر · إيقاف بثّ الوسوم كـ keywords · حذف isAccessibleForFree · تكرار التواريخ.
- [ ] **40.** تدقيق الكود الميت — يدوي، «شك واحد = احتفظ».
- [ ] **41.** بنود Client Edit / Impersonation المعلقة (git: `CLIENT-EDIT-IMPERSONATION-PENDING.md`).
- [ ] **42.** تحقق المشاريع القديمة الكبيرة — CLIENT-PAGE-FULLSITE · CLIENTS-TODO · STORY-PAGE · SUBSCRIBER-TO-CLIENT-CONVERSION (git history).
- [x] **44.** ربط العميل بالكاتب (Editor) — **مبني + تست حي ناجح، بانتظار push** (2026-07-26). المنفّذ: `Client.editorId → Staff` (علاقة `StaffEditorClients`) · اختيار الكاتب في **الشريط السفلي الثابت** لتعديل العميل + في نموذج الإنشاء · عمود Editor في **جدول العملاء** + **جدول المقالات** (موروث من العميل) + فلتر pills بالكاتب (All/كاتب/No editor). **بلا** skills/Categories (خالد ألغاها — تعقيد زائد). خارج JSON-LD، الناشر يبقى مدوّنتي. أُنشئ كاتب تست `editor-test@modonty-test.local` (EDITOR) للتست على الإنتاج. + **جدول المقالات هُوجِر لمعيار الأدمن**: `DataTable` المشترك (#3) بدل الجدول اليدوي + فلتر الكاتب بـ`CountTab` المشترك (#1، رُفِع لـ`components/admin/count-tab.tsx`). tsc admin صفر أخطاء · console صفر أخطاء.
- [ ] **43.** استثمار `admin/lib/gsc/indexing.ts` (كود يتيم — لا نحذفه). **ما هو:** غلاف Google Indexing API v3 (`requestIndexing` URL_UPDATED · `notifyDeleted` URL_DELETED + نسخ batch + `getRemovalMetadata`). مناديه انحُذفوا سابقاً لأن الـ API الرسمي مقصور على `JobPosting` + `BroadcastEvent` فقط (لا صفحات عامة) — مؤكَّد من مصادر Google. **كيف نستفيد منه مستقبلاً (يا تيم):** (أ) لو أضفنا نوع محتوى **وظائف/فعاليات** لأي عميل → نربطه فوراً لفهرسة فورية شرعية. (ب) `getRemovalMetadata` (read-only، كوتا منفصلة) يصلح كـ«هل أرسلنا هذا الـ URL للحذف؟» فحص تدقيقي بلا استهلاك كوتا الكتابة. (ج) قالب جاهز (JWT + scope + batch concurrency) لأي Google API كتابة قادم. **قبل أي تفعيل:** تأكيد أن نوع المحتوى ضمن ما يسمح به الـ API (وإلا يُفلتر بصمت).

---

## 📌 قواعد التشغيل
- كل بند له **رقم مرجعي ثابت** — خالد يعطي الرقم فقط. المنجز يُحذف والباقي يبقى برقمه.
- «reminder X» → يُضاف فوراً كبند مرقّم هنا.
- تقارير مريم → بنودها المفتوحة تدخل هنا.
- ملاحظات التست الحي → `documents/tasks/CLAUDE.md` (خام)، والقابل للتنفيذ ينتقل هنا.
- أي بند يخلص → **يُحذف** (بلا قسم Done، بلا حشو) + تحديث Last Updated.



### 60. ✅ `sharp` لا يُحمَّل داخل سيرفر الأدمن على ويندوز — **أُصلح** (2026-07-31)

**كان العرض:** أي رفع صورة من `/media/upload` يفشل **صامتاً** — لا تنبيه، لا حفظ. الـaction يرجع في ~100ms.

**السبب الجذري (مثبَت):** `next@16.2.9` يجلب `sharp 0.34.5` كـ`optionalDependency`، والمشروع كان يطلب `sharp ^0.35.3`. الاثنان يشحنان `libvips-42.dll` **بنفس الاسم وإصدارين مختلفين** (libvips 8.17 مقابل 8.18.3). ويندوز يحمّل الـDLL بالاسم مرّة واحدة لكل عملية → أول من يُحمَّل يفوز، والثاني يفشل بـ`ERR_DLOPEN_FAILED: The specified procedure could not be found` (خطأ ويندوز 127).

**كيف تحقّقنا:**
- نفس ملف `.node` يعمل ١٠٠٪ في Node عادي — ولّد صورتَي الغلاف بنجاح.
- يفشل داخل Next حتى على سيرفر أُعيد تشغيله نظيفاً → ليست حالة كاش.
- `serverExternalPackages: ["sharp"]` كان مضبوطاً أصلاً — المشكلة ليست فيه.
- رفع مباشر على Bunny بالـAPI نجح (201 Created) → ليست مشكلة مفاتيح ولا شبكة.
- مخزن pnpm أظهر النسختين جنباً إلى جنب.

**الإصلاح المطبَّق:** `pnpm.overrides.sharp = "0.34.5"` في `package.json` الجذر + تثبيت `admin/package.json` على نفس النسخة. اخترنا نسخة Next نفسها (لا العكس) حتى لا نمسّ مُحسِّن صور Next؛ واستخدامنا (`metadata` · `resize` · `webp` · `toBuffer`) متطابق في الإصدارين.

**التحقّق بعد الإصلاح:** بقيت نسخة واحدة `@img+sharp-win32-x64@0.34.5` · الرفع صار يأخذ 2061ms (رفع حقيقي) · `createMedia` كتب `bunnyUrl` · الصورتان تُخدمان بـHTTP 200.

**الإنتاج لم يكن متأثراً:** لينكس يحمّل `.so` بمسار كامل وsoname مُصدَّر — التصادم ويندوزي بحت.

**أثر جانبي متبقٍّ (بند منفصل):** فشل الرفع كان **لا يُظهر أي تنبيه** — `waitForUploadCompletion` يبتلع الخطأ فيبدو الزر كأنه لم يُضغط. أُضيف `console.error` تشخيصي في `upload-image-to-bunny.ts` (يبقى). يستحق إصلاح UI لاحقاً.

---

### 59. ✅ غلافا «التقويم الهجري» و«كأس العالم 2026» — **استُبدلا** (2026-07-31)

الصورتان الأصليتان كانتا محذوفتين من Cloudinary (HTTP 404) فتعذّر ترحيلهما. وُلِّد غلافان جديدان 1920×1080 ورُفعا من واجهة الأدمن (لا سكربتات) على `modonty_dev`.

| المقال | الغلاف الجديد | الحالة |
|---|---|---|
| التقويم الهجري | `post/مدونتي/cover-hijri-calendar.webp` | HTTP 200 ✅ |
| كأس العالم 2026 | `post/مدونتي/cover-world-cup-2026.webp` | HTTP 200 ✅ |

نص بديل ووصف سيو مكتوبان للاثنين. **متبقٍّ:** إعادة توليد السيو المخزّن للمقالين + للمؤلف `modonty` (بند ٦١).
