# ✅ TODO — ملف المهام الوحيد (مرتّب: السهل → الصعب)

> **القاعدة (خالد 2026-07-08):** هذا هو ملف الـ TODO الوحيد. أي مهمة/«reminder»/بند مريم يدخل هنا. ممنوع ملف TODO جديد.
> **ما ليس TODO يبقى بمكانه:** `documents/reels/` · `documents/content-team/` · المراجع · `documents/tasks/CLAUDE.md` · SESSION-LOG.

**Last Updated: 2026-07-25**

---

## 🟡 متوسط — ساعات (مرتّب: الأسهل + الأأمن أولاً)

### آمن — جهد أكثر (سكيما محلية / عدّة نقاط)
_(لا بنود حالياً)_

### تحقق / بانتظار قرار (مو كود جديد)
- [ ] **تقرير الأسئلة — تحقق ببيانات حقيقية** (`modonty_dev` صفر أسئلة زوّار). اطرح سؤالاً من مودونتي المحلي ثم افتح `/analytics/leads/questions`.
- [ ] **انحرافا موكب الرئيسية:** صف أفعال الزوار قبل بطاقات GA4 + فلتر التاريخ — **مباركة خالد** أو إعادة هيكلة.
- [ ] **navbar — لفظ «اشترك مجاناً» ملتبس** (البديل «سجّل مجاناً»/«أنشئ حساب» — `LoginButton.tsx:47` + `FeedTopBanner.tsx`). **محجوب — يتبع قرار العضوية.**
- [ ] **تعبئة الـ٢٠ عميلاً الباقين** (فواتير/تواريخ). **قرار خالد:** مدة موحّدة أم لكل عميل؟
- [ ] **«متأخّر» حقيقي** → يلزم حقل `dueDate` على `Invoice` (مؤجّل).
- [ ] **`prisma db push` (CTA):** فهرس `labelKey @unique` بالسكيما، غير مدفوع للإنتاج؛ الحماية بحارس بالأكشن. **يُنفَّذ وقت النشر.**

### 🟠 حسّاس — كتابة على الإنتاج (dry-run + backup إلزامي)
- [ ] **إصلاح البراند المزدوج في العناوين** (`metadata-generator.ts`) + **Regenerate شامل** (كل المقالات).
- [ ] **ربط الـ١٣ عميلاً القدامى بـ CTA** (Run-All تطابق بالنص المطبَّع → ١٢ تلقائي + ١ يدوي «عمرو مصطفى»). idempotent، تكتب `ctaPresetId` فقط.
- [ ] **مزامنة الإنتاج بعد rename على dev:** تسمية من dev تخلي الأصل القديم 404 عالمياً؛ شغّل التسمية في نفس بيئة العرض.
- [ ] **تنظيف المخزّن — نظافة المحتوى (Run-All step):** يمسح ألوان inline من `content` على **كل المقالات**. الأحسّاس: dry-run + idempotent + backup.
- [ ] **🔴 صفحة `/clients/[slug]/book` لا تُنتج حجوزات** (٣٩ فتحة/صفر حجز، كل العيادات ٠٪). الشك: `disclaimerAccepted` الإلزامي يمنع الإرسال. تحقيق على الإنتاج.

## 🔴 كبير — مشاريع + قرارات
- [ ] **مراجعة سكيما شاملة + سياسة تكلفة مونجو** (تاسك كبير — خالد 2026-07-24): جرد نمو الجداول (page_views/analytics/cta_clicks/…) · تداخل GA4 (دفع مزدوج) · فهارس TTL (`sessions`/`system_errors`/`audit_logs`) · فهارس زائدة · موديلات ميتة (`SeoIntake`/`JbrseoSubscriber`/`Subscriber` vs `NewsSubscriber`/`intake_*`) · حقول JSON منتفخة. المخرجات: تقرير جرد + سياسة احتفاظ + سكربت TTL (mongosh، إنتاج بإذن + باكب). **⚠️ بلا حذف حتى نراجع سوا** ([[feedback_check_datalayer_env]]).
- [ ] **صلاحيات الأدمن لكل دور** (الثغرة الحادّة انسدّت: `checkAdmin`+`proxy.ts` = طاقم نشط فقط). كل الأدوار تشوف كل شي، وعمليات الفلوس (`createInvoice`/`mark-paid`) بـ`auth()` لا role-gate. الخطة `ADMIN-PERMISSIONS-PLAN-v1.html` — خالد يقفل ٥ قرارات (تقييد SALES لعملائه + إخفاء الأقسام + قفل الفلوس).
- [ ] **«المهتمون» — قرارات + بناء:** توحيد الموديلات الثلاثة (`NewsSubscriber`/`Subscriber`/`JbrseoSubscriber`) · حذف بوكس نشرة المقال · واجهة إطلاق الحملة بالأدمن (استهداف بالموضوع/الصناعة فوق الموافقين) · مفتاح الحملات في `notificationPreferences` + تشيك بوكس التسجيل (غير مؤشّر افتراضياً — GDPR/PDPL) + استعلام الاستهداف.
- [ ] **GEO/AEO — التنفيذ** (الفحص انتهى، `GEO-AEO-AUDIT-2026-07-13.html`): سكور AEO (مرحلة ٢) · قياس AI referrals (classify-source + enum) · تقرير Bing · PII في `/users/[id]` · hreflang يُستبدل لا يُورَّث.
- [ ] **الريلز — الجهة الإدارية** (مركونة): reels→GA4 (الفيد لا يرسل أحداث) · تست رفع الكونسول · موافقة الأدمن · ترحيل gallery القديمة · مرحلة الفيديو · قرار الكوتا. `documents/reels/`.
- [ ] **تيليقرام:** الحدث الميت المتبقّي `leadHigh` **محجوب** (لا lead-scoring في مودونتي بعد) — `campaignInterest` صار مربوطاً (`register-interest.ts:105`). + مرآة الأدمن الكاملة + سجل أحداث موحّد. (22/26 يعمل.)
- [ ] **معرض العميل — راوت مستقل + تخزين أوفر:** راوت يعرض معرض كل عميل (إضافة+حذف) · شيله من `/media` (بلا كسر `/seo-images`) · نقله لـ Bunny (Cloudinary مكلف — آمن بمعمارية Media ID).
- [ ] **دفع بقية الدفعة الثانية:** كونسول (gallery→Bunny يحتاج نموذج Reel) + مودونتي (geo tracking + booking_submit + reels) + نماذج Reels بالسكيما. قبلها tsc+builds+تست. 🔔 قبل أي دفع modonty: Lighthouse + GZIP + الباندل.

## 🟢 الباك لوق — مؤجّل (مشاريع/قرارات/أفكار)
- [ ] **جلسة فريق المحتوى** — 7/11 قرار مفتوح. `CONTENT-TEAM-BRAINSTORM-v1.html`.
- [ ] **نظام الروابط المختصرة** — 9 مراحل + 3 قرارات (git: `SHORT-LINK-SYSTEM-TODO.md`). يتقاطع مع QR.
- [ ] **جرد الإيميلات الشامل** — كل trigger/قالب عبر الثلاثة + Resend (حوافز النشرة الست «قريباً»).
- [ ] **مُراجِع YMYL** — الطبيب الموافق بالكونسول = المُراجِع (`reviewedById`). المؤجل: MEDICAL-YMYL (~32س JSON-LD MedicalWebPage). `YMYL-FLOW-DISCUSSION.md`.
- [ ] **صيانة DB داخل Run-All:** decay Analytics/ArticleView (365ي) · إشعارات (30ي) · articleBodyText · NewsSubscriber ملغي (6ش) · Conversion/CampaignTracking · العدادات · يتيمة (يدوي).
- [ ] **إعادة تصميم بوابة جودة المقال** — قائمة الكاش تكذب + خلط «محسوب» و«ناقص».
- [ ] **Hero 1200×630:** رفع القديمة 6:1 + كروبر الكونسول + `hero-cover.tsx` (3:1→6:1).
- [ ] **روابط داخلية سياقية داخل جسم المقال** (سيو داخلي).
- [ ] **dateModified** للمقالات.
- [ ] **مراجعة خبير لكلمات الرئيسية** (homepage) بعد النشر.
- [ ] **Google Reviews للـ Premium** — Places API (بلا AggregateRating + بلا كاش).
- [ ] **نظام Reviews لمودونتي** (آراء العملاء على modonty.com).
- [ ] **تدقيق فاتورة Vercel** — `vc> audit`.
- [ ] **بطاقة عروض الشركاء** — MediaType OFFER + قرار validUntil.
- [ ] **QR code لكل عميل** — يتقاطع مع الروابط المختصرة.
- [ ] **Admin Broadcaster متعدد القنوات** (فكرة).
- [ ] **إعادة تصميم صفحة العميل بالأدمن** + intake للقراءة + نشاط العميل.
- [ ] **تكبير شعارات شركاء النجاح** بالرئيسية.
- [ ] **قسم «الجديد» من قنوات السوشيال** (فكرة).
- [ ] **قسم خدمات العميل** — موديل ClientService.
- [ ] **تدقيقات تنظيف JSON-LD:** حذف `Client.keywords` (~33 موقع) · بطاقة ClientKeyword بالمحرر · إيقاف بثّ الوسوم كـ keywords · حذف isAccessibleForFree · تكرار التواريخ.
- [ ] **خطة حذف seoKeywords** — 17 موقع، 6 مراحل (git: `SEOKEYWORDS-DELETION-EXECUTION.md`).
- [ ] **تدقيق الكود الميت** — يدوي، «شك واحد = احتفظ».
- [ ] **بنود Client Edit / Impersonation المعلقة** (git: `CLIENT-EDIT-IMPERSONATION-PENDING.md`).
- [ ] **تحقق المشاريع القديمة الكبيرة** — CLIENT-PAGE-FULLSITE · CLIENTS-TODO · STORY-PAGE · SUBSCRIBER-TO-CLIENT-CONVERSION (git history).

---

## 📌 قواعد التشغيل
- «reminder X» → يُضاف فوراً كبند هنا (بالمستوى المناسب).
- تقارير مريم → بنودها المفتوحة تدخل هنا.
- ملاحظات التست الحي → `documents/tasks/CLAUDE.md` (خام)، والقابل للتنفيذ ينتقل هنا.
- أي بند يخلص → **يُحذف من الملف** (بلا قسم Done، بلا حشو) + تحديث Last Updated.
