# مهام معلّقة بقرار خالد

> يُقرأ عند كل `hh>`. البند يُحذف من هنا لحظة حسمه، لا يُعلَّم صحّ.

---

## 🔴 ١ · اختبار «المقال ما فتحت» على التست — أوّل شيء عند العودة

**الحالة:** الإصلاح مدفوع على `test` (`376a95e`)، والنشر على `test.modonty.com`.
**المطلوب:** تصفّح ١٠–١٥ مقالاً **مع تسجيل دخول** — العطل لا يظهر لغير المسجَّل،
ولهذا كانت كل قياسات كلود بـ`curl` خضراء بينما خالد يرى العطل.

**قرار خالد المطلوب — واحد من ثلاثة:**

| | الخيار | ماذا يلزم |
|---|---|---|
| أ | خالد يعطي كلود حساب قارئ على التست | إيميل + باسورد |
| ب | خالد يأذن لكلود يسجّل حساباً بنفسه من `/users/register` | كلمة «سجّل» |
| ج | خالد يختبر بنفسه من جوّاله | لا شيء |

**ما يفعله كلود بعدها في كل الحالات:** يقرأ `system_errors` على قاعدة الإنتاج،
يصنّف ما ظهر بالمسار والوقت و`category`، ثم يحكم: `RSC-CONNCLOSED` يُقفل أو
يُفتح تحقيق جديد. **و`test → main` لا يُطرح إلا بأمر خالد الصريح.**

---

## ٢ · نصوص مشاكل الداتا الثلاث (تبويب طارق)

تنتظر **قرار خالد النصّي** لا شغلاً تقنياً:
- عناوين خمس صفحات بلا `seoTitle`: `contact` · `trust` · `story` · `reels` · `audio`
- أسماء ثلاث فئات مختلطة، وأصعبها «Modonty جديد مدونتي»
- بند الكاتب انحلّ: خطوة صيانة `Author Identity Arabized` جاهزة، تحتاج ضغطة `Run All`

---

## ٣ · الريلز — أُغلقت وحُذف تبويبها ٢ سبتمبر، وبقي منها ثلاثة

المراحل ١ و٢ و٣ و٤ سُلّمت ومُقيست (`0547780`). التفصيل في `SESSION-LOG.md` بلوك ٢ سبتمبر.

| | البند | مَن يحسمه |
|---|---|---|
| أ | يبقى العميل قادراً يسحب ريله المنشور من الكونسول، ولّا السحب بيدنا وحدنا؟ | **خالد** — سلوك مصمَّم لا عطل (`console/.../reels-manager.tsx:322`) |
| ب | زرّ «حذف نهائي» داخل شاشة الريلز | **خالد** — الطريق قائم اليوم من مكتبة الوسائط بعد الأرشفة، وبناء ثانٍ يعني مسارَي حذف يتفرّقان |
| ج | فحص دوريّ يقارن `Content-Length` بما يصل فعلاً — على الريلز **المنشورة سابقاً** | كلود، متى أُذن — حارس الاعتماد الجديد لا يحرس القديم |

**وبند لا يخصّنا:** أربعة ريلز تنتظر رفعاً جديداً من العملاء (`REEL-REUPLOAD`) — ثلاثة
`status 0 · storage 0` ورابع ملفه ناقص. لا زرّ يصلحها من عندنا؛ العميل يرفعها فتظهر
في «بالانتظار» مكتملة.

---

## ٤ · T9 — إطفاء Cloudinary (مؤجَّل عمداً، لا معلَّق)

نُقل من تبويب «لاحقاً» في لوحة الدمج ٢ سبتمبر ٢٠٢٦ عند حذف التبويب.
الوسائط كلها على بني من زمان، وCloudinary باقٍ حساباً مفتوحاً لا يقرأ منه أحد.
**وهو مجاني** (خالد ٢ سبتمبر) — فلا كلفة تستعجل إغلاقه، ويبقى شبكة أمان حتى تطمئنّ.
مؤجَّل بقرار خالد منذ ٧ أغسطس ٢٠٢٦.

---

## ٥ · نطاق الـCheckup

اللوحة فيها **١٧ بنداً** في «شغل مفتوح» — منها ثلاثة مقيسة مفتوحة (`47b` · `25` · `37`)
والباقي يحتاج فتحاً. خالد يقرّر: نمشي عليها بالترتيب، أم ننتقي بالأولوية؟

---

## ٦ · بعد اطمئنان ١٠٠٪ فقط

- حذف `modonty-ui` (شبكة الأمان الحالية)
- حذف الفروع الميتة الأربعة: `client-articles` · `modonty-site-groups` · `seo/phase-0-cleanup` · `version-2`
- **النسختان الاحتياطيتان `main-backup-2026-08-31` و`main-backup-2026-08-02` تبقيان** بأمر خالد
