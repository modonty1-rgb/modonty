# Arabic Pure Prose — moved to a global skill

This spec is **universal** (applies to all of Khalid's projects and interfaces), so it now lives in one place only, at global scope:

**`~/.claude/skills/arabic-prose/SKILL.md`** — the single source of truth (full three-tier model, examples, edge cases, checklist, v1.1.0).

It auto-loads on `tr>` or when writing Arabic content. The short always-on summary is in `~/.claude/CLAUDE.md`.

_(This file was the original authored spec — kept as a pointer to avoid two copies drifting apart. Edit the skill, not this file.)_
