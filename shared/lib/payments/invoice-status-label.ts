import type { InvoicePaymentStatus } from "@prisma/client";

/**
 * **اسمُ حالة الفاتورة — قائمةٌ واحدة لكلّ الشاشات** (الأدمن · الكونسول · الإيميل).
 *
 * مربوطةٌ بالـenum (`Record<InvoicePaymentStatus, …>`): قيمةٌ جديدةٌ في السكيما بلا اسمٍ هنا
 * خطأُ ترجمة، لا شاشةٌ تطبع رمزاً إنجليزيّاً. وكانت كلُّ شاشةٍ تكتب اسمَها بيدها، فقال كشفُ
 * الحساب عن الطلب «مدفوع» وعن فاتورته «مسدَّدة» (خالد ٢٣ سبتمبر ٢٠٢٦: «ارتباك»).
 *
 * «مدفوعة» لا «مسدَّدة»: الكلمةُ نفسُها التي يقولها الطلب (`PAID → مدفوع`)، مؤنّثةً للفاتورة.
 */
export const INVOICE_STATUS_LABEL: Record<InvoicePaymentStatus, string> = {
  PAID: "مدفوعة",
  DUE: "بانتظار الدفع",
};
