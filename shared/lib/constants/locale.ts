/**
 * لغة تنسيق الأرقام والتواريخ — مصدر حقيقة واحد للتطبيقات الثلاثة.
 *
 * هذه القيمة تُمرَّر إلى `Intl` و`toLocaleString`/`toLocaleDateString` وحدها: هي التي
 * تجعل الرقم يُطبع `١٬٢٣٤` بدل `1,234` والتاريخ `١٥ يناير ٢٠٢٦` بدل `15/1/2026`.
 *
 * كانت مكتوبةً بيدها **١٦٠ مرّة** في المستودع (مقيس ٢٨ أغسطس ٢٠٢٦: مدونتي ٨٤ · الأدمن ٣٧ ·
 * الكونسول ٣٢ · المشترك ٧)، ومنها أربع مواضع كتبت `"ar-EG"` بلا سبب — والاثنان يعطيان
 * **ناتجاً متطابقاً** في الأرقام والتواريخ والأوقات (مقيس بـ`node`)، ولا يفترقان إلا في رمز
 * العملة، ولا واحد من الأربعة كان يمرّر عملة. فتوحّدت كلّها هنا.
 *
 * ⚠️ ليست `Settings.inLanguage` ولا `Settings.defaultOgLocale`:
 * - `inLanguage` = اللغة التي **نُعلنها** لجوجل في البيانات المنظَّمة — تُقرأ من القاعدة.
 * - `defaultOgLocale` = صيغة أوبن جراف (`ar_SA` بشرطة سفلية) — تُقرأ من القاعدة.
 * - وهذه = لغة **الطباعة على الشاشة**، ونداؤها متزامن وسط الرسم في ٥٤ ملفاً منها مكوّنات
 *   عميل، فقراءتها من القاعدة تجعل كل واحد منها `async` وتكلّف نداءً لكل زائر بلا مقابل.
 *   وتوحيدها هنا هو الخطوة الأولى لو أُريد ربطها بالقاعدة يوماً — يُعدَّل سطر واحد لا ١٦٠.
 *
 * لتغيير شكل الأرقام والتواريخ في كل المستودع: عدّل هذا السطر وحده.
 */
export const SITE_LOCALE = "ar-SA";

/**
 * نفس اللغة بالتقويم الميلادي مثبَّتاً. `ar-SA` وحدها تطبع التاريخ **هجرياً** — تقويمها
 * الافتراضي `islamic-umalqura` — فأي تاريخ ميلادي (تاريخ نشر، تاريخ سجلّ تجاري) يحتاج هذه.
 */
export const SITE_LOCALE_GREGORIAN = `${SITE_LOCALE}-u-ca-gregory` as const;

/**
 * نفس اللغة بأرقام لاتينية (`1234` لا `١٢٣٤`) — للأرقام التي تُقرأ كمعرّفات لا ككمّيات،
 * مثل سنة التأسيس في بطاقة الشريك.
 */
export const SITE_LOCALE_LATN = `${SITE_LOCALE}-u-nu-latn` as const;
