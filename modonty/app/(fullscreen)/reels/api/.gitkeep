مجلّد قياسي — يبقى فارغاً حتى يُملأ.

نقاط عامّة يناديها عميل خارجي (تطبيق الجوّال أوّلاً). `route.ts` هنا ينادي
**نفس الدالّة** في `data/` التي ينادیها السيرفر كومبوننت — منطق واحد وبابان.

`page.tsx` و`route.ts` لا يتشاركان مقطعاً واحداً، لذلك الملفّ في مجلّد فرعي.
