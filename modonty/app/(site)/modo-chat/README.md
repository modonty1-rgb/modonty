# modo-chat

The `/modo-chat` page: ask Modo about an article or a category, answered from platform
content first and the web only as a fallback.

Start at `documentation/MAP.md` — one line per file, plus the traps already hit here.
Open work lives in `TASK.md`.
