This folder contains **shared date and time formatting components**.

Examples:
- Human-readable date components
- Relative time / "time ago" displays

Keep only generic, reusable date/time UI here. Route-specific date formatting should live in that route's own `components/` folder.

