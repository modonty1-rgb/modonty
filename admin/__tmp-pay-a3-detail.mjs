import { chromium } from '@playwright/test';
const browser = await chromium.launch();
const context = await browser.newContext({ viewport: { width: 1280, height: 900 }, storageState: 'C:/tmp/pay-a1-evidence/state.json' });
const page = await context.newPage();
await page.goto('http://localhost:3001/commercial-plans/6aa163e18a00cef23ea91b97', { waitUntil: 'networkidle' });
const bodyText = await page.locator('body').innerText();
console.log('contains المدد والهدايا:', bodyText.includes('المدد والهدايا'));
await page.screenshot({ path: 'C:/tmp/pay-a3-evidence/detail-page.png', fullPage: true });
await context.close();
await browser.close();
console.log('DONE');
