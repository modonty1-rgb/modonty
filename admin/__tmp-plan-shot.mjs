import { chromium } from '@playwright/test';
const browser = await chromium.launch();
const page = await (await browser.newContext({ viewport: { width: 1440, height: 1000 } })).newPage();
const errs = []; page.on('pageerror', e => errs.push(String(e)));
await page.goto('file:///c:/Users/w2nad/Desktop/dreamToApp/MODONTY/documents/tasks/JBR-PAY-DASH.html');
await page.waitForTimeout(400);
await page.screenshot({ path: 'C:/tmp/ultra-ui-evidence/plan-top.png' });
await page.locator('#grp-pay-plan').scrollIntoViewIfNeeded();
await page.waitForTimeout(300);
await page.screenshot({ path: 'C:/tmp/ultra-ui-evidence/plan-phases.png' });
console.log('cards:', await page.locator('.card').count(), '· errors:', errs.length);
await browser.close();
