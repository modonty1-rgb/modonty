/**
 * السوقان اللذان نبيع فيهما — تعريفٌ واحد بدل ثلاثة.
 *
 * كانت `COUNTRY_LABEL` مكتوبةً في الجدول وفي عمود بيانات العميل، و`MOBILE` في السكيما تحمل
 * تسميتيها الخاصّتين. ثلاث نسخٍ لاسمين لا يتغيّران — وأوّل تعديلٍ على إحداها يفتح فرقاً صامتاً
 * بين شاشتين.
 *
 * والقائمة ثابتة عمداً ولم تصر جدولاً كالمصادر: السوق يقرّر شكل الجوّال وعملة الصفقة ونطاق
 * الفوترة، فإضافته قرارٌ في الكود لا صفٌّ يُضاف من شاشة.
 */
export const MARKETS = ["SA", "EG"] as const;

export type Market = (typeof MARKETS)[number];

export const MARKET_LABEL: Record<string, string> = {
  SA: "السعودية",
  EG: "مصر",
};

/** مفتاح «بلا سوق» — علامةُ شاشةٍ لا رمز دولة، فلا تُشبه أيّ سوقٍ حقيقيّ. */
export const NO_MARKET = "__none__";

/**
 * نقطة لون لكل سوق — من عَلَمه لا من لوحة الحالات.
 *
 * أخضر السعودية وأحمر مصر يُعرفان بلا تعلّم، بخلاف لونين محايدين يحتاجان حفظاً. والالتباس
 * مع أعراف الحالة (`red` = حرج) لا يقع هنا: النقطة تجاور **اسم سوق** داخل بطاقةٍ عنوانها
 * «السوق»، لا تجاور حالةً ولا رقماً.
 *
 * ويبقى للمرحلة لونُها في `STAGE_DOT` — بُعدان مختلفان، ولكلٍّ نقطته في بطاقته.
 */
export const MARKET_DOT: Record<string, string> = {
  SA: "bg-emerald-500",
  EG: "bg-red-500",
};

/**
 * حافّة البطاقة بلون السوق — تمييزٌ يُقرأ في المسح لا في القراءة.
 *
 * النقطة تحتاج أن تقع عليها العين؛ والحافّة تمتدّ بارتفاع البطاقة كلّها فتُفرز القائمة إلى
 * كتلتين بلا قراءة حرف. وهي عرفٌ قائم في الأدمن لا اختراع: جدول «الحسابات» يلوّن صفوفه
 * بـ`border-s-<tone>-500` (معيار الكيانات #٣).
 *
 * و`border-s` لا `border-r`: الحافّة البادئة — يمينٌ في العربية ويسارٌ في الإنجليزية.
 */
export const MARKET_EDGE: Record<string, string> = {
  SA: "border-s-emerald-500",
  EG: "border-s-red-500",
};
