import { redirect } from "next/navigation";

/**
 * **تحويلٌ إلى `/migrations`** (خالد ١٩ سبتمبر ٢٠٢٦).
 *
 * صار للترحيلات صفحةٌ جامعة حين أُضيف ترحيلُ الوثائق، فاسمُ هذا الراوت وصف ترحيلاً
 * واحداً من اثنين. ويبقى محوِّلاً لا محذوفاً: الرابطُ في السايدبار ربّما حُفظ في
 * مفضّلة أحدهم، وصفحةُ ٤٠٤ على أداةٍ تُستعمل مرّةً تجعلها تبدو مفقودة.
 */
export default function OrdersMigrationRedirect() {
  redirect("/migrations");
}
