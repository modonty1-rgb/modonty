import { chromium } from '@playwright/test';
const BASE = 'http://localhost:3001';
const browser = await chromium.launch();
const context = await browser.newContext({ viewport: { width: 1280, height: 900 }, storageState: 'C:/tmp/pay-a1-evidence/state.json' });
const page = await context.newPage();
await page.goto(`${BASE}/commercial-plans`, { waitUntil: 'networkidle' });
await page.locator('summary:has-text("إضافة باقة جديدة")').click();
await page.waitForTimeout(200);
await page.fill('input[name="name"]', 'باقة اختبار PAY-A4');
await page.fill('input[name="sa"]', '111');
await page.fill('input[name="eg"]', '1111');
await page.fill('input[name="articlesPerMonth"]', '2');
await page.click('button:has-text("إنشاء مسودة")');
await page.waitForLoadState('networkidle');
await page.waitForTimeout(500);
console.log('created');
await context.close();
await browser.close();
