// Default password assigned to every new client on creation.
// Delivered to the client via the welcome email; the client is required to
// change it from the console on first login. Admins never set a password.
export const DEFAULT_CLIENT_PASSWORD = "admin123";
